# AI 生成声明（AI-Generated Code Declaration）

> **一句话**：本项目**自身**的全部代码与文档**均由 AI 生成**；人类一方负责提出需求、验收行为与决定发布。
>
> 适用范围：`server/`、`web/`、`scripts/`、`installer/`、`assets/` 内的清单文本、以及本仓库的全部 Markdown 文档。
> 不适用：第三方代码与数据、标准许可原文（见 §3）。本声明与 `LICENSE`（MIT）**并行有效**，不改变也不削减其条款。

---

## 1. 声明

**本项目的全部代码均由人工智能（AI）生成。** 后端、前端、启动与打包脚本、安装器配置与文档，都是在
AI 助手（对话式编码智能体）中逐轮生成并修改的产物；人类一方负责提出需求、验收行为、决定发布与是否继续，
**没有逐行重写这些代码**。

逐块口径：

| 范围 | 内容 | 是否 AI 生成 |
|---|---|---|
| 后端 | `server/*.js`、`server/util/*.js`（Node 标准库，**零 npm 依赖**） | ✅ 是 |
| 前端 | `web/*.js`、`web/pages/*.js`、`web/i18n/*.json`、`web/styles/*.css` | ✅ 是 |
| 脚本 | `scripts/*.ps1`、`scripts/*.js`、`scripts/*.cjs`、`start.cmd` | ✅ 是 |
| 配置 | `package.json`、`installer/*.json`、`.gitignore`、`.gitattributes` | ✅ 是 |
| 文档 | `README.md`、`FEATURES.md`、`MIGRATION.md`、`THIRD_PARTY.md`、`docs/*.md`、本文件 | ✅ 是 |
| 本项目许可全文 | `LICENSE`（MIT 标准文本） | ❌ 否（标准许可原文） |
| 随包携带的第三方原件 | `web/vendor/react*.js`（React 18.3.1 UMD）、`assets/artists/*.txt`（画师清单，MIT） | ❌ 否（上游原件） |
| 不随包分发的外部组件与权重 | ComfyUI 本体（GPL-3.0）、Anima / Qwen 系权重、llama.cpp、7-Zip、Danbooru 角色词表 | ❌ 否（由向导在用户机器上从上游获取） |

> 生成方式的可核对性：代码里保留了逐轮改动的原因与实测结论（`docs/FULL-REFERENCE.md` 的决策记录 ①–㉝ 与各轮附录、
> `docs/HANDOVER.md` §13 轮次摘要），并配有自带自检（`node scripts/check.js`，当前 **17/17**）。这些都是"能自己验一遍"的入口，
> 而不是只凭本声明背书。

## 2. 使用者的责任（请务必阅读）

1. **不附带任何担保**：与 `LICENSE` 的 MIT 条款一致，本项目按 **"现状（AS IS）"** 提供，作者与分发者不对适用性、
   正确性、完整性或由此产生的任何后果负责。AI 生成的代码同样如此，**不因为"由 AI 生成"而获得额外保证或额外免责**。
2. **请自行审查**：把它当作可读、可改、可审的源码。用于生产、再分发或商用之前，建议自行完成代码审查与安全评估
   （尤其是：监听地址、局域网令牌、外接 API Key 的存放与日志边界，见 `docs/FULL-REFERENCE.md` §9）。
3. **以实测记录为准，不要凭本声明判断可靠性**：哪些跑过、哪些**没有**端到端跑过，都如实写在
   `docs/FULL-REFERENCE.md`（§13 验证记录与各轮附录）与 `docs/HANDOVER.md`（§10 已知限制、§7 验证手册）里。
4. **遵守上游许可**：模型权重与 ComfyUI 本体各有自己的许可（Anima 系为**非商业**），详见 §3。

## 3. 第三方边界

本项目**只调用、不内联**第三方：ComfyUI（GPL-3.0）不随包分发、仅由向导从官方渠道获取；Anima / Qwen 系权重按各自上游许可
（非商业 / 研究用途）；React UMD 与画师清单为 MIT；角色词表在运行时下载。
**本声明不改变、也不能改变这些上游许可的义务** —— 完整清单与分发义务见 `THIRD_PARTY.md`、`LICENSES/README.md` 与 `README.md` §12。

---

## English

**All of this project's own code and documentation was generated by AI.** The backend, the frontend, the launcher and
packaging scripts, the installer configuration and every Markdown document were produced and revised by an AI assistant
(conversational coding agent sessions). The human side set the requirements, accepted or rejected the behaviour, and decided on
releases — it did not hand-write the code line by line.

**Not covered by this declaration:** the standard MIT licence text (`LICENSE`), the vendored upstream files
(`web/vendor/react*.js`, `assets/artists/*.txt`), and everything fetched from upstream at runtime (ComfyUI under
GPL-3.0, the Anima / Qwen weights under their own licences, llama.cpp, 7-Zip, the Danbooru character index). See
`THIRD_PARTY.md` and `LICENSES/`.

**No warranty.** The project is provided "AS IS", without warranty of any kind, consistent with the MIT licence in
`LICENSE`; being AI-generated grants no additional guarantee and no additional exemption. Review the code yourself
before production, redistribution or commercial use. Which checks were actually run — and which were not — is recorded
honestly in `docs/FULL-REFERENCE.md` (§13 and the appendices) and `docs/HANDOVER.md` (§10).
