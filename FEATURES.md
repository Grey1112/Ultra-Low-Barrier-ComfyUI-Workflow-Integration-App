# 功能指南（FEATURES）

> 本文档面向**使用者与运维者**：把每个功能拆成「做什么 → 怎么用 → 边界/降级行为」，方便按需查阅与排障。
>
> 配套阅读：`README.md`（快速开始与总览）、`docs/INTERNAL-CONTRACT.md`（前后端契约）。
> 项目内所有位置都以 `<项目根>` 指代项目文件夹；命令一律写成 `pwsh -File .\scripts\start.ps1` 这样的相对路径形式。

---

## F1 工作台与独立生图

### 1.0 三栏工作台（生图 / 本地 LLM / 画师同屏）

**做什么**：把原来**三个独立页面**（生图、本地 LLM、画师）合并成**同一页三栏并排**，写提示词与出图在同一屏完成。左侧导航因此只剩 **🎛 工作台 / ⚙ 设置 / 🚀 向导** 三项。

| 栏 | 类名 | 内容 |
|---|---|---|
| 左栏 | `.wb-gen` | 生图面板（就是 F1 下文各小节讲的那块） |
| 中栏 | `.wb-llm` | 本地 LLM / 外接 API（F2） |
| 右栏 | `.wb-art` | 画师收藏 / 黑名单 / 检索（F4） |

**怎么用**：

| 控件 | 行为 |
|---|---|
| 顶部「**布局：三栏 / 两栏 / 仅生图**」 | 切换显示几栏；选择存在浏览器 `localStorage` 的 `dcp-workbench-layout`（**纯界面偏好，不写进 `data\settings.json`**）；隐私模式下写不进去也不报错 |
| 每栏标题条上的 **▾ / ▸** | **折叠 / 展开**该栏（折叠后只留标题条，把空间让给其它两栏） |
| 各栏内部滚动 | 每栏**独立滚动**、互不挤压：生图栏滚面板自身，LLM / 画师栏整栏滚动 |

**边界/降级行为**：

- **窗口窄时自动降级**，纯 CSS 断点、缩放窗口即时生效（不靠 JS 判断）：
  - `< 1560px`：三栏布局**收起画师栏**；
  - `< 1180px`：降到**单列**（两栏布局下会直接收起 LLM 栏）。
- 三栏是三列网格，列宽按内容比例分配（生图最宽、画师最窄），各列有最小宽度（`minmax`）以免塌陷；
- 生图面板没加载成功时（`/static/panel.js` 不可访问），左栏显示「生图面板未加载：请确认 /static/panel.js 可访问」，其余两栏照常可用；
- 在窄列里 LLM 栏会自动改为上下单列、画师栏的胶囊/列表高度收窄（由 `web/styles/workbench.css` 覆盖，不改各页面自身样式）。

### 1.1 三条管线：模型 → 编码器 → VAE 的配对与崩点

**做什么**：扩散模型是**主选择器**，选了模型，管线、文本编码器、VAE、条件节点与采样器默认值都自动跟着走。

**怎么用**：在工作台**左栏（生图）**顶部的模型下拉里选一个 `.safetensors`，下面三个下拉会自动跳到配套项；也可以手动改（改错会在提交前被拦下）。

| 管线 | 扩散模型（`diffusion_models`） | 文本编码器 | VAE | 潜空间通道 | 条件节点 / 采样器 |
|---|---|---|---|---|---|
| **Anima 3.8B**（v2 bundle） | `Anima-3.8B-v1.1.safetensors` | `qwen_3_06b_base.safetensors`（1024 维，"原生编码器"） | `qwen_image_vae.safetensors` | **16** | `Anima38BV2Prompt` + beta/KSampler |
| **Anima 通用** | `anima-turbo-v1.1` / `anima-base-v1.0` / `anima-aesthetic-v1.1` / `Anima-2.9B-preview-v1` / `Anima-2.9B-preview-v1_int8_convrot` | `qwen_3_06b_base.safetensors`（1024 维） | `qwen_image_vae.safetensors` | **16** | `CLIPTextEncode` + `KSampler(er_sde/simple)` |
| **Qwen-Image 2.1** | `qwen_image_2.1_int8_convrot.safetensors` | `qwen3vl_8b_w4a8.safetensors`（**4096 维**） | `qwen_image_2.1_vae_bf16.safetensors` | **64** | `TextEncodeQwenImage21` + `KSampler(euler/simple)` |

**派生依据（不是猜文件名）**：

- Anima 3.8B 路线看 `Anima38BV2Loader` 节点自己的 combo —— 那个 combo 是节点 mmap 扫 `diffusion_models\*.safetensors` 的 safetensors metadata（`architecture == anima_3_8b_semantic_connector_v2_bundle`）得来的，所以"某文件出现在这个 combo 里"本身就是一条 safetensors 级判据；
- 其余权重按 ComfyUI 自己的 `detect_unet_config` 判定：四份 Anima 权重都判成 `image_model='anima'` → 16 通道潜空间（`latent_format=Wan21`）；文件名含 `qwen[-_]?image` 的走 Qwen-Image 2.1；
- 参数默认值也跟模型走：Anima 3.8B 默认 40 步 / CFG 6，Anima 通用 30 步 / CFG 4，Qwen-Image 2.1 25 步 / CFG 1；文件名含 `turbo` 的权重覆盖为 **CFG 1 / 10 步**。

**两个明确的崩点**（面板会把原始报错 + 人话解释一起给你）：

| 崩点 | 现象 | 原因 | 处理 |
|---|---|---|---|
| **VAEDecode 通道数错误** | 解码时报通道数不匹配 | 扩散模型的潜空间通道数 ≠ VAE 能解码的通道数（Anima 16、Qwen-Image 2.1 64） | 换成同管线配套的 VAE |
| **编码器维度错误** | `RuntimeError: Given normalized_shape=[4096], expected input with shape [*4096]` | `[4096]` 是 Qwen-Image 2.1 文本投影要求的维度，实际喂进去的是 1024 维 —— 即把 Anima 的原生编码器给了 Qwen-Image 2.1（或反过来） | Anima 系 → `qwen_3_06b_base.safetensors`；Qwen-Image 2.1 → `qwen3vl_8b_w4a8.safetensors` |

**边界/降级行为**：

- **提交前硬闸门**：不兼容组合（模型/VAE 通道数、编码器维度）**不会静默提交**，界面直接点名两个文件与原因；闸门只拦"实证不兼容"的组合，不阻止其它组合；
- 缺少必需节点时（Anima 3.8B 缺 `Anima38BV2Loader` / `AnimaQwen35Loader`，或其它两条管线缺 `UNETLoader`）会给出"缺少 XX 节点"的原因而不是一句"开始生成"；
- ComfyUI 离线时模型下拉为空，页面提示需要先启动 ComfyUI。

### 1.2 多张逐张生成

**做什么**：一次出 1–20 张，**逐张串行生成**，每张有自己的画师 tag 与自己的种子。

**怎么用**：设置「张数」→ 点生成。实时行显示「第 i/n 张 · 状态 · 已耗时」，每张跑完即落盘；点「中止」可随时停。

**边界/降级行为**：张数会被收敛到 1–20；每张独立随机画师时，开始前会提示本次模式与总张数；中止后已完成的图保留在缩略图条里。

### 1.3 参考图（img2img）

**做什么**：以一张本地图片为底图做 img2img。

**怎么用**：

1. 点 **🖼 选择参考图**（上传到 ComfyUI 的 `dsh-panel-ref` 目录）→ 出现缩略图；
2. 调 **重绘幅度（denoise）**，范围 0.05–1.00，默认 0.75；
3. 点生成。生效时**宽/高输入自动忽略**；
4. 点 **✕ 清除** 回到纯文生图。

**边界/降级行为**：

- 支持性判据：`/object_info` 里**同时**具备 `LoadImage` 与 `VAEEncode` 节点，**且**当前管线已选定 VAE —— 缺一不可；
- 不满足时**参考图自动无效**：界面给出**点名缺什么**的原因，本次**不携带参考图字段**，按纯文生图生成（**不拦截生成**）；
- 未使用参考图时，生成的图与"没有 img2img 功能"时**逐字节等价**：零新增节点、零接线改动，denoise 保持字面 1；
- 上传失败会显示「参考图上传失败：<原因>」，不会静默吞掉。

### 1.4 剪贴板「📋 一键替换」

**做什么**：读系统剪贴板，按关键词自动区分正向/负向提示词并替换。

**怎么用**：把带 `Positive prompt:` / `Negative prompt:` 标签的文本复制到剪贴板 → 点提示词区右侧的 **📋 一键替换**。

**边界/降级行为**：

- 剪贴板 API 不可用（无权限、非安全上下文）时，界面**降级**出一个**面板内粘贴框**（`剪贴板不可用，请在此粘贴 (Ctrl+V)`），手动粘贴后确认使用；
- 剪贴板为空会提示「剪贴板是空的」，不会清空你原来的提示词。

### 1.5 实时预览 / 缩略图 / 显存

**做什么**：生成过程可视化 + 资源可视化。

**怎么用 / 能看到什么**：

| 元素 | 说明 |
|---|---|
| 实时预览 | 生成中优先显示采样实时预览帧，跑完切回落盘的最终图 |
| 实时行 | 「实时预览 / 生成中」标签 + 「第 i/n 张」+ 状态 + 已耗时 |
| 进度条 | 当前这张的采样进度 |
| 缩略图条 | 多张时出现；点缩略图切换大图 |
| 大图 | 点一下在新标签页打开原图 |
| 显存 / 内存 | 显示 `显存 已用 / 总量 GB` 与占用条 |
| 释放显存 | 卸载模型并清空显存缓存；生成中点击会提示「生成中，暂不释放显存」 |

**边界/降级行为**：实时预览依赖面板自己的 WebSocket 中继（同源，不直连 ComfyUI 端口）；ComfyUI 离线时区域显示「启动 ComfyUI 后开始生图」。

### 1.6 内嵌与外接双模式

| 模式 | 目录 | 说明 |
|---|---|---|
| **内嵌（embedded）** | `<项目根>\runtime\comfyui` | 由向导下载/导入官方便携包；启动时用其自带的 `python_embeded\python.exe` |
| **外接（external）** | 你在设置里填的目录 | 使用已有安装；探测 `main.py` 与 `venv\Scripts\python.exe` / `python_embeded\python.exe` / `python.exe` |

**怎么用**：向导第 1 步选择，或「设置 → ComfyUI → 运行模式」。外接模式填完目录点「检测」，界面会显示识别出的**布局**（`portable` / `venv`）与**权重目录**。

**边界/降级行为**：

- 外接目录记录的是**绝对路径**，换机器/换盘符后可能失效；自检会报 `comfy-dir-missing` 并给出修复建议（重新指向或改用内嵌模式），**只报告不阻断启动**；
- 外接目录不存在、或找不到 `main.py`、或找不到 Python 解释器时，「启动 ComfyUI」会返回明确原因，不会静默失败。

### 1.7 旧收藏导入

**做什么**：把插件版存在浏览器 `localStorage` 的收藏（key `dcp-artist-favs`）一次性迁到服务端。

**怎么用**：打开独立版时外壳**自动检测并导入**（成功后 toast 提示导入了几位）；也可以在「工作台右栏（画师）→ 从浏览器旧数据导入」手动触发。

**边界/降级行为**：浏览器里没有旧数据属于正常情况，静默跳过；旧数据不是合法 JSON 数组时视为失败并提示，不会写坏服务端数据。导入是**合并**语义（已存在的不会重复计入 `imported`），且会顺带把同名 tag 从黑名单移出（保持互斥）。

---

## F2 本地 LLM / 外接 API / 角色词表 / 提示词工具

> F2 分四块：**本地模型（预置默认 + 推荐目录）**、**外接 API（纯聊天内核）**、**角色词表（中文别名 + 输出补全）**、**提示词工具（一键填入 + 一键复制）**。
> 前两块是"推理来源"的二选一（本地 llama.cpp / 外接 OpenAI 兼容接口），后两块对两种来源都生效。

### 2.1 运行时：llama.cpp 子进程

**做什么**：在本机跑一个 `llama-server` 子进程，用 OpenAI 兼容接口做流式对话。

**怎么用**：「工作台 → 本地 LLM 栏 → 安装运行时」→ 选 **自动 / CUDA 版 / CPU 版** → 等进度条跑完（自检会显示版本号与来源）。之后用「启动服务 / 停止服务」控制它。

**边界/降级行为**：

- **自动**会探测本机是否有 NVIDIA GPU（`nvidia-smi -L`）：有 → CUDA 版，无 → CPU 版；
- **CUDA 版装/启失败会自动回落到 CPU 版重试**（装的时候先试若干个 CUDA 资产，再试 CPU；启动时先试带 `--no-webui` 的参数组，失败换一组）；
- CUDA 版缺 `cudart64*.dll` 时会自动补下 cudart 运行库；
- 两级都失败才报错，错误文案里会点明"常见原因：CUDA 运行库缺失或显存不足 —— 可在「本地 LLM」页改装 CPU 版运行时"；
- 运行时目录：`<项目根>\runtime\bin\llama\`；日志：`<项目根>\logs\llama-server.log`。

### 2.2 模型：预置默认 + 推荐目录（一键下载）

**做什么 / 是什么**：预置 **`Qwen3.5-2B-UD-Q5_K_XL.gguf`**（约 1.4 GB），放在 `<项目根>\models\llm\`；旁边多了一张**推荐模型目录** `installer\llm-models.json`（7 条），在「本地 LLM」栏里**一键下载 / 设为默认**。

**推荐目录（字节数与许可都已实测核对；`modelscope` 字段标明该文件在 ModelScope 有没有同字节镜像）**：

| 文件 | 字节数 | 许可 | 说明 |
|---|---|---|---|
| `Qwen3.5-9B-UD-Q4_K_XL.gguf` | **5,966,095,584** | Apache-2.0 | **默认推荐（★）**：与内置 2B 同族、参数量 4.5 倍，中文理解与常识明显更好；ModelScope 有镜像；**已实测 8 GB 显存可跑**（全量上卡、12.3 s 就绪；sha256 与远端一致） |
| `Qwen3.5-9B-Q4_K_M.gguf` | 5,680,522,464 | Apache-2.0 | 标准 Q4_K_M，比 `UD-Q4_K_XL` 更省一点显存 |
| `Qwen3.5-9B-UD-Q5_K_XL.gguf` | 6,743,680,224 | Apache-2.0 | 更高精度，8 GB 显存偏紧 |
| `Qwen3-8B-Q4_K_M.gguf` | 5,027,784,512 | Apache-2.0 | 上一代通用 8B 备选，显存最宽裕 |
| `BooruNL-0.8B-Q6_K.gguf` | 629,743,872 | Apache-2.0 | 专门做「自然语言 → Danbooru **外观** tag」，但模型卡明确说明**不输出角色/画师 tag** |
| `ggml-model-Q6_K.gguf`（DanTagGen-delta-rev2） | 323,077,792 | **CC-BY-SA-4.0** | 真正的 Danbooru tag 生成器，但是「tag 进 → tag 出」；**带 share-alike** |
| `mmproj-F16.gguf` | 918,166,080 | Apache-2.0 | 多模态投影文件（可选），**本版未接入 UI** |

**怎么用**：「本地 LLM」栏右侧「推荐模型（一键下载）」卡片 → 点 **下载**（走 F3 下载策略，有进度弹窗）→ 下完按钮变成 **设为默认**。

**边界/降级行为**：文件已存在时按钮直接显示 **设为默认**（不必重下）；「设为默认」同时写设置 `llm.defaultModel` 与 `data\llm\models.json`。

> **9B 实测（本轮末补齐）**：ModelScope 下完并 **sha256 校验一致**；8 GB 显存（ComfyUI 同时在线）`-ngl 99` 全量上卡、**12.3 s 就绪**；角色问题答 `rem`，整段提示词里自己写出 `rem_(re:zero)`。踩到的坑也记在这里：**第一次断点续传补出来的文件字节数正确、内容却是坏的**（回答全是 `?`），重下后正常 —— 所以下载路径现在带 **sha256 校验**（对不上删文件报错），并多了「校验完整性」按钮。详见 `docs/FULL-REFERENCE.md` 附录 I/J。

> **预置 2B 模型的边界（重要）**：这是一个**速度优先**的 2B 量化模型，**遵循指令的能力有限** —— 可能不按模板只写一个围栏、可能把负向 tag 塞进正向、可能在负向里刷一长串近义词。程序因此做了输出规范化（见 §2.8），并会**明确提示**做了哪些处理。

**想要更高质量**：优先用上面的推荐目录（9B）；也可以在「模型管理」里「添加本地路径」或「下载模型」换成别的 GGUF，「设为默认」后重启服务。

**边界/降级行为**：默认模型文件缺失时自检报 `llm-model-missing`，启动服务会给明确错误；列表里第一个可用模型会在未显式设置默认时自动成为默认。

### 2.3 模型管理

| 操作 | 怎么用 | 边界/降级行为 |
|---|---|---|
| **添加本地路径** | 填 `.gguf` 路径 → 选 `link` / `copy` → 点添加 | 只接受 `.gguf`；`link` 同卷用**硬链接**，跨卷自动回落为复制；文件不存在会明确报错 |
| **下载模型** | 填下载地址 + 文件名 → 点下载 | 走 F3 下载策略（官方源 → 镜像）；有进度、失败有明确报错；**可以直接粘贴网页链接**（HF 的 `/blob/`、ModelScope 的 `/file/view/`、ModelScope 旧式 `?Revision=&FilePath=` 都会先被规范成直链，见 F3） |
| **设为默认** | 列表行上的「设为默认」 | 当前默认项按钮为禁用态；设为默认会同时写入设置与模型清单 |
| **移除模型** | 列表行上的「移除模型」→ 确认框 | 移除默认模型后自动切到剩下的第一个；文件不存在会报错 |
| 列表信息 | 文件名、大小、来源（预置/已添加/已下载）、是否默认、是否 abliterated | 文件名含 `abliterated` / `uncensored` / `heretic` 时自动打「已去审查」标记 |

模型统一放在 `<项目根>\models\llm\`，元数据记在 `data\llm\models.json`。

### 2.4 abliterated（溶解版）搜索

**做什么**：经 **HF 镜像**检索 abliterated / 去审查 GGUF。

**怎么用**：「搜索模型」输入关键词（留空则默认搜 `abliterated GGUF`）→ 点搜索。结果按仓库展示（仓库名、下载量、许可、文件列表），每个文件有「下载」按钮。

**边界/降级行为**：

- **上游确实没有就如实说**：返回的说明是"镜像上没有检索到匹配的 GGUF。上游可能确实未发布「溶解版（abliterated）」权重 —— **本程序不会伪造或改名充数**"，并建议改用「从本地路径添加」；
- 检索不可达时提示"模型检索不可达（HuggingFace 与镜像都没响应）"，建议手动下载后本地添加；
- 结果里会附带一条许可提醒：abliterated 版本**只在本地使用，禁止再分发**（涉及 Qwen Research 等许可）。

### 2.5 上下文 5 条与可调 0–20

**做什么**：只有最近 N 条对话被送进模型。

**怎么用**：默认 **5**；在「设置 → 本地 LLM → 上下文条数」改成 **0–20**（0 = 完全不带历史）。LLM 页标题行显示当前生效值，每条回答下方显示本次实际用了多少条。

**边界/降级行为**：**系统提示词不计入 N**（它每轮都在，且在消息列表最前面）；服务端只保留最近 N 条（`n=0` 时不保留任何上下文）；超出范围的值会被夹紧到 0–20。

### 2.6 「新建会话」（= 新开对话，v1.2.1 起不再删历史）

**做什么**：开一段**全新**的对话（清空界面与上下文），**但不删除任何旧会话** —— 旧会话连同完整消息留在本机，随时可以从侧栏「历史对话」里点回来。

**怎么用**：LLM 页聊天框右侧的「新建会话」按钮；右侧「历史对话（永久保留）」卡片列出全部会话（首条用户消息摘要 + 条数 + 更新时间），点条目载入整段，点「删除」才会真的删掉那一条（弹窗二次确认）。

**边界/降级行为**：一次动作做三件事 —— ① 分配一个新的会话 id（形如 `t…`），旧 id 原样保留；② 取消正在进行中的流式回答并清空界面消息与思考块；③ 本地 provider 下顺带擦掉 llama.cpp 的 slot 推理缓存（外接 API 没有这个概念）。历史为空时自动等同"新开对话"。删除当前会话后自动接回最近更新的那一条；删空时界面回到空态。

### 2.6.1 历史永久保留与自动接回（v1.2.1）

**做什么**：用户端历史**永久保留**（除非用户主动删除）。刷新页面、切换标签页、重开程序都会自动接回**上一次的对话**。

**怎么用**：打开工作台就能看到上次聊到哪；想找更早的会话，点右侧「历史对话」列表里任意一条。

**边界/降级行为**：数据落在 `data/llm/sessions.json`（随项目迁移）；每个会话最多留 `keepMessages` 条消息（默认 40，设置页可调 2–2000，只影响界面回看/复制，不影响"发给模型的上下文条数"）；列表接口只下发摘要（条数/首条摘要/时间），正文按需拉取，避免一次拉几十 MB；`lastSessionId` 随会话保存，删掉当前会话时按更新时间回落到最近一条。

### 2.7 系统提示词固定为原文

**做什么**：模型的身份与输出格式约束完全由一份文本文件决定。

**怎么用**：LLM 页「系统提示词」卡片点「显示」，即可看到当前生效的**原文**；它固定取自 `<项目根>\assets\templates\anima-system-prompt.txt`。

**边界/降级行为**：程序**不追加任何其它身份、工具或安全说明**；文件缺失时接口直接报错「内置系统提示词缺失」（并提示恢复该文件），**不会**用一段内置兜底文本蒙混过去。改了文件重启后端生效。

### 2.8 提示词工具：一键填入 + 一键复制，以及输出规范化

**怎么做**：回复下方是「**提示词工具（解析结果 → 面板）**」卡片 —— 解析**最后一条**助手回复 → 正/负向各一个**只读预览框** → 每个框各有 **📋 复制** 与 **⬅ 填入**，卡片底部还有 **填正负**、**📋 复制正+负**、**📋 复制整段回复**。

| 按钮 | 行为 | 边界/降级行为 |
|---|---|---|
| **⬅ 填入**（正向 / 负向各一个） | 把解析出的那一段写进生图面板对应输入框 | 该段为空时按钮置灰；面板未挂载时提示失败，**不会**用空串覆盖你已有的提示词 |
| **填正负** | 两段一起填 | 缺任意一段时提示「回复里缺少需要的那一段」并放弃 |
| **📋 复制**（正向 / 负向各一个） | 复制该段到剪贴板 | 优先 `navigator.clipboard.writeText`，失败回落隐藏 `textarea` + `document.execCommand('copy')`；两路都失败提示「复制失败」 |
| **📋 复制正+负** | 按 `Positive prompt: …` 空行 `Negative prompt: …` 的格式复制两段 | 没有可用回复时置灰 |
| **📋 复制整段回复** | 复制模型回复原文 | 同上 |
| **📋 复制**（角色检索结果行） | 复制该条 Danbooru tag，用于手填提示词 | 见 §2.10 |

**填入的实现要点**：填入走面板自己暴露的 `window.__DCP_PANEL_API__.setPrompts()` —— 是**真正的 React `setState`**，所以提示词会真的参与生成，而不是只改 DOM 显示；成功后会切到生图栏并提示已填入几段（提示由外壳统一发）。

**解析规则与一个真实缺陷的修复**：优先取**最后**一个同时含 `Positive prompt:` 与 `Negative prompt:` 的围栏块；取不到围栏时退回整段文本。**本轮修掉的缺陷**：旧写法在回复**以闭合的 ``` 结尾**时，会把"结尾之后的空串"也当成一个围栏，而解析取的是**最后一个**围栏 → 正/负向永远为空 →「填入 / 复制」按钮全部置灰。修法：只收**有内容**的围栏；只有反引号数量为**奇数**（确实未闭合）且尾巴有内容时，才把尾巴当候选。解析不出内容时按钮置灰并说明原因，**不会乱填**。

**输出规范化（针对小模型的兜底，全部会告示）**：

| 处理 | 触发条件 | 告示方式 |
|---|---|---|
| 只取**第一个**代码围栏 | 模型给了多个围栏 | 界面直接替换为规范化后的内容（`normalized` 标记） |
| 同一行内**按逗号去重** | 正/负向里有重复 tag（大小写不敏感、保序） | 同上 |
| **冗余截断** | 超过 **120 个 tag** 或 **800 字符**（小模型爱刷"词汤"） | 追加提示："已省略 N 处冗余内容（模型有堆砌/复读倾向…）" |
| 正向里的负向起手词截断 | 正向段中出现 `worst quality` / `low quality` / `jpeg artifacts` / `blurry` 且不在开头 | 同"规范化"告示 |
| 正负向去重 | 同一 tag 同时出现在正向与负向 | 从负向里剔除 |
| 长度截断提示 | 回答触达长度上限 | 追加提示："回答触达长度上限被截断（可再点一次生成，或把描述写得更短）" |
| 只有思考内容 | 模型没给正文、只给了 `reasoning_content` | 原样返回思考内容，并注明"模型只输出了思考内容（未产出正文），已原样返回" |
| 识别不出结构 | 没有可识别的 `Positive/Negative prompt:` 标签 | **原样返回原始输出**，不伪造内容 |

**边界/降级行为**：回答长度有硬上限（默认 512 token）以免"会思考"的模型把上下文吃光。以上规范化对**本地模型与外接 API 两条路径都生效**。

### 2.9 外接 API（推理来源 = 外接 API）

**做什么**：不装本地运行时也能用 —— 把「推理来源」切到**外接 API（OpenAI 兼容）**，对话走外接服务的 `/chat/completions` 流式接口。**只做纯聊天**：一段系统提示词（仍是 `assets/templates/anima-system-prompt.txt` 原文）+ 最近 N 条对话，**不挂工具、不挂检索、不挂推理内核**。

**怎么用**：「设置 → 本地 LLM → 推理来源」选 **外接 API**，填下表字段，点「**测试连接**」确认，然后照常在 LLM 栏聊天。

| 字段 | 取值 | 说明 |
|---|---|---|
| **API 地址（baseUrl）** | URL，例如 `https://api.deepseek.com/v1` | 也支持本机 `http://127.0.0.1:8199/v1`（用本机 `llama-server` 当 OpenAI 兼容端点） |
| **API Key** | 字符串 | 只写入 `<项目根>\data\settings.json`（已 gitignore）；**不进日志、不进交付文档**；页面用密码框 |
| **模型名** | 服务端要求的 id，例如 `deepseek-chat` | 必填 |
| **温度 / 单次上限** | 0–2（默认 0.6）/ 64–32768（默认 1024） | 单次回答 token 上限 |

**「测试连接」做了什么**（`POST /app/llm/api/test`）：先试 `/models`（多数服务支持，返回里的模型列表会被截到 200 条），**再发一条极短对话**（`messages:[{role:'user',content:'ping'}]`、`max_tokens:8`、非流式），返回 `{ok, ms, model, baseUrl, hasKey, models, sample}`；配置不完整时直接报「外接 API 配置不完整，缺少：baseUrl、model」。

**边界/降级行为**：

- **只校验 `baseUrl` 与 `model` 必填**，`apiKey` 允许为空（本机端点通常不需要）；
- 错误映射会写进文案：`401/403` → API Key 无效或没权限；`404` → baseUrl 或模型名不对（多数服务要写到 `/v1` 这一层）；`429` → 触发限流，稍后再试；连不上 → 提示检查地址、网络与代理；
- 外接路径**不需要**本地 llama.cpp 运行时，也不需要任何 GGUF；「运行时安装 / 模型管理」卡片仍会在，切回本地模型时才用得上；
- 外接路径同样会做输出规范化与**角色 tag 补全**；完成帧里带 `provider: "api"`（本地路径带 `provider: "local"`）。

> **实测**：用本机 `llama-server`（`http://127.0.0.1:8199/v1`）当 OpenAI 兼容端点，`/app/llm/api/test` 返回成功，`/app/llm/chat` 走外接路径返回流式正文（见 `docs/FULL-REFERENCE.md` 附录 H）。

### 2.10 角色词表与中文别名（角色识别兜底 + 输出补全）

**先说结论（有实测依据）**：8 GB 显存能跑的文生文模型**都不能可靠地**把「蕾姆」这类名字翻成 Danbooru 规范角色 tag ——

- 唯一的 NL→Danbooru 模型（**BooruNL-0.8B**）在模型卡里明确写「**不输出角色 / 画师 tag**」；
- 唯一的 Danbooru tag 模型（**DanTagGen**）是 **tag 进 → tag 出**，要先有人给出 characters 字段；
- 明确用 DanbooruTags 微调的权重是 **27B**，最小量化也放不进 8 GB。

所以本版的做法是「**更大的通用模型（9B）+ 确定性角色词表兜底 + 中文别名**」，而**不是**宣称某个模型能认出绝大部分角色。

| 组成 | 落位 | 说明 |
|---|---|---|
| **角色词表** | `<项目根>\data\characters\danbooru.csv` | 运行时下载 MIT 许可的 tagcomplete `danbooru.csv`：**3,518,020 B / 140,782 行**，其中 `category=4` 的角色 tag **40,931** 条（带 post 计数）；**懒加载**索引（首次用到才读、成功才缓存），另存一份 `index.json` 元数据 |
| **内置中文别名** | 代码内（`server/characters.js`） | **563 条**，例如 `蕾姆 → rem_(re:zero)`、`初音未来 → hatsune_miku`、`甘雨 → ganyu_(genshin_impact)`；长别名优先匹配。**563 条全部逐条核对过 Danbooru 词表**（自检 0 条未命中），并刻意排除「时 / 天天 / 天使 / 真理 / 琴 / 小美 / 白露 / 悠悠 / 陈 / 玛丽 / 吉尔 / 忧」这类常用中文词 |
| **用户别名** | `<项目根>\data\character-aliases.json` | 可扩展；在「角色词表」卡片里填「中文/日文名 → 对应 Danbooru tag 名」即可（用户项覆盖内置同名项） |

**怎么用**：

1. 「本地 LLM」栏 → **「角色词表（Danbooru 角色 tag 索引）」**卡片 → 点 **下载/更新词表**（约 3.5 MB，走 F3 下载策略；走 job + 进度弹窗）；
2. 卡片会显示「词表就绪」与「角色条目 40,931」；用 **「搜角色（中英均可）」**检索，结果行显示 tag 与 post 计数，点 **📋 复制** 拿到规范 tag；
3. 需要时在下方填一条中文别名并保存。

**输出补全（显式告知，不是静默改内容）**：模型回答里缺规范角色 tag 时，后端按词表**补进正向提示词**（保持"逗号分隔单行"的既有格式），并回一帧 `charactersAdded`，界面显示一条系统提示「**已按角色词表补全角色 tag：…**」。

**边界/降级行为**：

- 词表**没装**不影响其它功能：`resolveFromText` 在索引缺失时仍能命中**别名表**（此时直接给出别名目标 tag），但不再做 post 计数排序与英文名解析；
- 补全前会做**宽松去重**：回复里已出现该角色（下划线/空格、括号形态都算，或裸名作为独立 tag 出现）就不再补；
- 解析顺序：**别名表命中**（长别名优先）→ **词表直接命中**（英文名，1–3 个词的组合，要求 post 计数 ≥ 20，避免把普通单词当角色）；
- 下载失败会**逐来源报错**（3 个来源：GitHub raw → jsDelivr → hf-mirror 上的同源镜像），解析出的条目少于 500 条会判为"疑似不是角色词表"并换源。

---

## F3 下载策略

**做什么**：所有下载（ComfyUI 本体、模型权重、画师清单、角色词表、LLM 运行时与模型、自定义节点）走同一套"官方源优先、镜像兜底、失败必报"的策略。

### 3.1 判定与切换

| 上游 | 官方源 | 镜像兜底 |
|---|---|---|
| HuggingFace（`huggingface.co` / `hf.co`） | 原 URL | ① 换成 `hf-mirror.com`（默认；可在设置里改）；② **ModelScope**：把 `/{owner}/{repo}/resolve/{rev}/{path}` 改写成 `modelscope.cn/models/{owner}/{repo}/resolve/{rev}/{path}`（`rev=main` 时会**同时试 `master`**）；可用 `download.useModelScope` 关掉，仓库不存在就立刻 404 并自然跳到下一个候选 |
| GitHub（`github.com` / `githubusercontent.com` / `codeload.github.com`） | 原 URL | 逐个尝试代理列表（默认 `gh-proxy.com` → `ghproxy.net` → `ghfast.top` → **`ghps.cc`**，按顺序；四个都实测能取到 llama.cpp 资产） |
| pip | 官方 index | 清华源（默认 `pypi.tuna.tsinghua.edu.cn`，可在设置里改），另有阿里云兜底 |
| ComfyUI 本体 | GitHub Releases / git / codeload | 便携包走 GitHub 代理；git clone 也逐个试代理 |
| 角色词表 | GitHub raw（tagcomplete 的 `danbooru.csv`） | jsDelivr 同源镜像 → hf-mirror 上的同源镜像 |

> **官方源不再"绝对优先"**：设置页有 **「一键填入国内优选源」** 按钮，一次把 HuggingFace 镜像设为 `hf-mirror.com`、打开 ModelScope、补齐四个 GitHub 代理、把停滞阈值设为 30 KB/s，并关掉"官方源优先"（`download.officialFirst`）。

**候选来源列表会打进任务日志**：每次下载开头都有一条

> `<文件名> 候选来源 N 个：官方源 → hf-mirror → modelscope → gh-proxy.com → …`

排障时一眼就能看出"到底试了哪些源、顺序如何"。

**切换条件（全部来自设置，不写死在代码里）**：

| 阈值 | 键 | 默认 | 作用 |
|---|---|---|---|
| 连接超时 | `download.officialTimeoutMs` | **10000 ms** | 只约束**建立连接**阶段；超时即换下一个候选 |
| 慢速阈值 | `download.slowThresholdKBs` | **200 KB/s** | 用于在**前几个候选之间"挑更快的"** |
| 慢速判定窗口 | `download.slowWindowMs` | **30000 ms** | 判定要用满整个窗口（见下） |
| **停滞阈值** | `download.stallKBs` | **30 KB/s** | 真正"没有进展"的门槛；**最后一个候选只按它判** |

**慢速判定用窗口均速，不是瞬时采样**：镜像常见"停顿几秒再冲一段"的突发式传输，瞬时采样会把这类源误判成"持续低速"并掐断。实现上每 1.5 s 采样一次，保留窗口内的样本，只有**整个窗口都覆盖满 且 窗口均速低于阈值**时才判慢。进度文案里同时给出「窗口均速」与「瞬时」，便于判断。

**最后一个候选只按停滞阈值判**：否则"所有源都慢"时会把本来能下完的文件一路掐掉。实测国内镜像常年只有一两百 KB/s，这条是必须的。

**明确提示**：一旦从官方源切到镜像并开始下载，界面/日志会出现 ——

> `<文件名>：官方源不可用（已重试），本次改用镜像来源 <host> —— 可在「设置 → 下载」里调整镜像与阈值`

**请求头与看门狗（都是实测踩出来的）**：

- **必须带 `User-Agent`**：不带时 ModelScope 的 CDN 会"连上但不吐数据"（0 字节卡死）；
- **一律带 `Range: bytes=<n>-`**（`bytes=0-` 等价全量）：ModelScope / hf-mirror 对 Range 请求才稳定返回 206 流，顺带**天然支持断点续传**；
- **无数据看门狗**：连上后 `20–60 s`（取 `slowWindowMs` 夹在 20–60 s 之间）一个字节都没动，直接判死换源；卡死的 body 有时不理会 `abort`，因此还会**显式销毁两端流**让 pipeline 立刻返回。

### 3.2 直接粘贴网页链接

「下载模型」「添加模型」的地址框可以直接粘贴**浏览器地址栏里的网页链接**，程序会先规范化成直链再下载：

| 粘贴的形态 | 规范成 |
|---|---|
| `huggingface.co/<o>/<r>/blob/<rev>/<path>` | `huggingface.co/<o>/<r>/resolve/<rev>/<path>` |
| `modelscope.cn/models/<o>/<r>/file/view/<rev>/<path>` | `modelscope.cn/models/<o>/<r>/resolve/<rev>/<path>` |
| `modelscope.cn/models/<o>/<r>?Revision=<rev>&FilePath=<path>`（旧式） | `modelscope.cn/models/<o>/<r>/resolve/<rev>/<path>` |
| 已经是 `/resolve/` 的直链（含 hf-mirror） | 原样不动 |

**边界/降级行为**：解析失败时保持原样（不猜、不改），交给下游下载策略处理并在失败信息里说明。**实测**：把 hf-mirror 指成不可达地址后，文件确实由 ModelScope 下完（任务日志可证）。

### 3.3 ComfyUI 本体

四条路径：① **便携包**（先问 GitHub Releases API 拿当前资产名，失败则按候选名 `ComfyUI_windows_portable_nvidia.7z` → `_nvidia_cu126.7z` → `ComfyUI_windows_portable.7z` 依次尝试，每个候选之间也会走镜像）；② **git clone**（逐个试代理，checkout 固定 commit，建 venv 并用 pip 镜像装依赖）；③ **codeload ZIP**（自定义节点在 git 失败后回落到这条路）；④ **本地归档 / 本地目录导入**（不走网络）。

### 3.4 进度、重试与报错

| 能力 | 说明 |
|---|---|
| 进度 | 长任务统一 Job + SSE：界面有进度条与逐行日志；每个任务在 `logs\jobs\` 下留一份 `.log` |
| 续传 | 下载写 `.part` 临时文件，支持 `Range` 断点续传；服务器不支持续传时自动丢弃断点重下并提示 |
| 重试 | **每个候选来源重试 2 轮**，全部失败才放弃；多候选（便携包、CUDA/CPU 运行时、代理列表）逐个尝试 |
| 跳过已就绪 | 目标文件大小与目录表一致时直接跳过（安装器可反复运行）；模型还有 sha256 校验 |
| 校验 | 大小不符或 sha256 不符会**删除文件并报错**（`文件已删除，请重试`） |
| 报错 | **绝不静默失败**：最终错误会列出**每个来源每次尝试的失败原因**，并提示去「设置 → 下载」调整镜像与阈值 |

**边界/降级行为**：

- 模型权重还能从"本地已有 ComfyUI 目录"里**硬链接/复制**过来（离线可用，且省一次全量下载）——同卷瞬时、跨卷自动回落为复制；导入后大小与目录表不一致会打**警告**但仍继续；
- 便携包全部候选不通时，错误里会直接建议：手动下载 Windows 便携包，然后在向导选「本地归档」指定该 `.7z`；
- 没有可用的 7-Zip 时会先获取 7-Zip 精简版 `7zr.exe`（约 0.6 MB，来自 7-zip.org），再用它解出 x64 版 `7za.exe`；这一步失败会回落到 `7zr.exe` 继续解压（更慢），系统装了 7-Zip 也能用；
- 解压后如果找不到可用的 ComfyUI（需要 `main.py` 与 `python_embeded\python.exe`），会报明确错误而不是留一个坏目录。

### 3.5 "测试镜像"探测

**怎么用**：「设置 → 下载源 → 探测镜像连通性」，对 `https://huggingface.co/` 的官方源与镜像分别发 HEAD 请求，结果以 toast 显示每个来源的**可达/不可达 + 状态码 + 耗时**。

**边界/降级行为**：只做可达性探测，不改设置、不下载；不可达时会显示具体错误信息。

---

## F4 画师收藏与黑名单增强

### 4.1 图上的三个按钮

生成出图后，图片下方显示该图使用的画师 tag（来自生成元数据，没有时回落到**落盘文件名**反推），以及三个按钮：

| 按钮 | 显式文案 | 行为 |
|---|---|---|
| **⭐ 收藏** | `⭐ 收藏：@xxx` / `⭐ 取消收藏：@xxx` | 加入/移出收藏；加入后可用「★ 随机收藏」档 |
| **🎯 选为画师** | `🎯 选为画师` | **切到「指定画师」模式并固定为这一位**（等同在搜索列表点选），并关闭"仅收藏"检索范围 |
| **🚫 拉黑** | `🚫 拉黑` / `🚫 取消拉黑` | 写入/移出黑名单；已在黑名单时 chip 上会显示 🚫 |

**边界/降级行为**：没有画师 tag 时这一行不出现；生成中不显示（按钮不可用）；每次操作都有 toast 说明结果。

### 4.2 选为画师 = 切到指定模式并固定

点「🎯 选为画师」后：`artistMode` 变为 `fixed`、`artistFixed` 变为该 tag、检索范围复位为"全部清单"，并提示「已选为画师：@xxx（指定模式，本次固定使用）」。之后想换，在画师区重新搜索点选或清除即可。

### 4.3 拉黑在所有随机档剔除

| 档位 | 黑名单是否剔除 | 说明 |
|---|---|---|
| 🎲 大随机 | ✅ 剔除 | 从 59,676 大池里过滤后再随机 |
| 🎲 小随机 | ✅ 剔除 | 从前 200 高频池里过滤后再随机 |
| ★ 随机收藏 | ✅ 剔除 | 从收藏里过滤后再随机 |
| 🔍 指定画师 | ❌ 不剔除 | **仍可搜到并标注「已拉黑」**，可手动取消拉黑 |

**边界/降级行为**：收藏里的画师如果**全部**被拉黑，会提示「收藏里的画师都被拉黑了，本次不注入画师（可在画师页取消拉黑）」，本次按 `noartist` 记文件名，**不拦截生成**。

### 4.4 指定模式的两种来源

| 来源 | 怎么用 | 说明 |
|---|---|---|
| **自定义画师** | 在「自定义画师」输入框直接写名字 → 点「使用 @名字」 | 输入即可用；自动规范化为 `@名字`（下划线转空格、只保留 Danbooru tag 允许字符）；**不做清单校验**，清单里没有的画师也能用 |
| **"仅收藏"检索** | 把检索范围切到「仅收藏（N）」→ 搜索 → 点选 | 池子换成你的收藏列表；切回「全部清单（59,676）」即回到大池 |

**边界/降级行为**：画师清单不可读时（`assets/artists\` 下缺清单 txt）界面提示「画师清单不可用：assets/artists/ 下缺少清单 txt（**自定义画师仍可直接输入使用**）」，随机档自动降级为"不注入画师"；被拉黑的画师在指定模式搜索结果里会带「（已拉黑）」后缀，选中后 chip 上也会标注。

> **v1.1.0 修复（用户报障）**：上面这条"两种来源"曾**只存在于文档与死代码里** —— 上一轮把画师管理挪到独立「画师」页时，把面板里的**搜索框与下拉一起删掉了**，只留下模式选项和一批无人调用的 state（`artistQuery` / `artistDropOpen` / `artistFavOnly` / `artistCustom` / `useCustomArtist`），于是选中「🔍 指定画师」后**根本无处可搜**。现已恢复，并且**收藏画师搜索**按需求做成开箱可用：
>
> | 步骤 | 行为 |
> |---|---|
> | ① 画师模式选「🔍 指定画师（搜索并从列表点选）」 | 出现搜索块：范围切换「全部清单（59,676）」/「仅收藏（N）」+ 搜索输入框 + 自定义画师输入框 |
> | ② 点「仅收藏（N）」再点搜索栏 | **列出全部收藏画师**（上限 300，超出可滚动），点 ★/☆ 可直接收藏/取消，🚫 可拉黑/取消拉黑 |
> | ③ 输入关键词 | 按名字子串过滤（归一化后匹配，忽略 `_`/转义与大小写），只显示匹配的画师 |
> | ④ 点一行 | 立即固定为该画师（`fixed` + `artistFixed`），搜索框清空、下拉收起，界面出现「已选画师：@xxx」chip |
> | 换回大池搜索 | 点「全部清单（59,676）」再输入，即在全量清单里按名字搜（这是用户报的"搜不了"的直接修复） |
>
> 收藏为空时列表区显示「收藏列表为空，本次不注入画师（可在搜索列表点 ★ 收藏）」；收藏范围下没有匹配项时显示「收藏里没有匹配的画师（可在「全部清单」里搜索并点 ★ 收藏）」—— 都不留空白让人猜。

### 4.5 收藏管理页

**怎么用**：工作台右栏（画师）四张卡片 ——

- **收藏**：chip 列表，每个 ✕ 移除；
- **黑名单**：chip 列表（样式区分），卡片底部说明"收藏与黑名单互斥"；
- **搜索**：关键词 + 范围 `全部` / `高频` / `收藏`，结果行有 `⭐/☆`（收藏切换）与 `🚫`（拉黑切换），被拉黑的带「已屏蔽」标记；回车即可搜索；
- **从浏览器旧数据导入**：读取 `localStorage` 的 `dcp-artist-favs` 合并导入，完成后提示导入了几位。

**边界/降级行为**：所有操作失败都会在页面上以红色错误条 + toast 呈现；`source=favs` 时检索的是收藏列表（搜索接口支持 `limit`，界面固定 50）。

### 4.6 互斥规则

**"收藏与黑名单互斥、后执行覆盖"**：同一 tag 加入一个列表时，会自动从另一个列表移除；切换（toggle）语义由服务端保证 —— 已在列表中则移出（`result: removed`），不在则加入（`result: added`），且加入时清理另一列表里的同名项。导入旧收藏时同样会把同名 tag 从黑名单移出。

**分组不是互斥的另一维**（v1.2.0 引入、v1.2.1 补齐入口）：同一位画师可以既收藏又同时属于多个分组；「分组随机」照旧剔除黑名单。

### 4.7 自定义分组：入口与写回（v1.2.0 引入，v1.2.1 补齐）

| 项 | 行为 |
|---|---|
| **入口（画师页）** | ① 搜索结果的每行「＋分组」；② **收藏 chip / 黑名单 chip 上也有「＋分组」**（v1.2.1 新增，旧版只能在搜索结果里加）；③ 分组卡片：建组、**展开看成员并逐个「移出本组」**、**重命名**、两步确认删除（后三项都是 v1.2.1 补的） |
| **入口（生图面板）** | v1.2.1：指定画师下拉的每一行有 🗂 按钮；已选画师 chip 行、「本次指定」区、「🎲 分组随机」块都能就地加组/移出/新建并加入 —— **不切页**。旧版面板完全没有加组 UI，这就是用户报的"图片部分根本不存在对应 ui 按钮" |
| **写回路径** | 面板只读 `window.__DCP_ARTISTS__.groups`；加/移出调外壳的 `window.__DCP_ARTIST_GROUP__`（服务端 `POST /app/artists/groups/add|remove`），新建组调 `__DCP_CREATE_GROUP__`，成功后 `__DCP_REFRESH_ARTISTS__` 刷新并广播 `dcp-artists-changed`，画师页与面板同时同步 |
| **为什么以前"建了组就没了"** | 面板挂载时会 `saveFavorites()` 一次；旧 `persistArtists` 把 favs/blacklist/groups **三份快照一起回传**，而 `__DCP_SAVE_ARTISTS__` 又无条件 PUT favs+blacklist —— 用户刚建的分组/黑名单被这次"顺手写回"抹掉。现在按 key 传、按 key 写（分组走 `POST /app/artists/groups/replace`，服务端仍做 50 组归一化） |
| **上限** | 最多 50 个分组（服务端 `MAX_GROUPS` 强制；建组输入框到上限即禁用并写明原因） |

---

## F5 设置页

**怎么打开**：左侧导航 → **⚙ 设置**。改完点「保存」，成功后有「设置已保存」的 toast，并立即刷新自检结果。

| 设置项 | 取值 / 范围 | 生效方式 |
|---|---|---|
| **界面语言** | `zh` / `en` | **即时切换并持久化**（写入设置、整树重渲染、面板文案由 DOM 翻译层跟上），无需重启 |
| **下载 → HuggingFace 镜像** | URL，默认 `https://hf-mirror.com` | 下一次下载生效 |
| **下载 → ModelScope 基地址** | URL，默认 `https://modelscope.cn` | 下一次下载生效 |
| **下载 → 把 HF 链接自动改写成 ModelScope** | 开关，默认**开**（`useModelScope`） | 下一次下载生效 |
| **下载 → GitHub 代理** | 逐行一个，按顺序尝试（默认含 `ghps.cc`） | 下一次下载生效 |
| **下载 → 官方源超时** | 毫秒，夹紧到 1000–120000（默认 10000） | 下一次下载生效 |
| **下载 → 慢速阈值 / 判定窗口** | KB/s（1–100000，默认 200）/ 毫秒（1000–600000，默认 30000） | 下一次下载生效 |
| **下载 → 停滞阈值 `stallKBs`** | KB/s（1–100000，默认 **30**）；最后一个候选只按它判 | 下一次下载生效 |
| **下载 → 一键填入国内优选源** | 按钮：hf-mirror + ModelScope + 四个代理 + 停滞 30 KB/s + 关掉"官方源优先" | 点完需点「保存」 |
| **下载 → pip 源** | URL，默认清华源 | 下一次 pip 安装生效 |
| **ComfyUI → 运行模式** | 内嵌 / 外接 | 保存后立即用于探测与启动 |
| **ComfyUI → 目录** | 外接模式下的 ComfyUI 根目录（含 `main.py`），有「检测」按钮 | 保存后生效 |
| **ComfyUI → 端口** | 1–65535，默认 8188 | 保存后生效 |
| **ComfyUI → 随面板自动启动** | 开关 | 保存后生效 |
| **监听地址 → 端口** | 1–65535，默认 8788 | 需重启面板 |
| **监听地址 → 允许局域网访问** | 开关 | 保存后**自动生成令牌**；后端改为监听 `0.0.0.0` |
| **本地 LLM → 推理来源** | `本地模型（llama.cpp）` / `外接 API（OpenAI 兼容）` | 下一轮对话生效（外接模式下不需要本地运行时/模型） |
| **本地 LLM → 外接 API** | baseUrl / API Key / 模型名 / 温度（0–2）/ 单次上限（64–32768）；另有「测试连接」按钮 | 保存后生效；Key 只写 `data\settings.json` |
| **本地 LLM → 上下文条数** | 0–20，默认 5（0 = 不带历史） | 下一轮对话生效 |
| **本地 LLM → 默认模型** | `models\llm` 下的 GGUF 文件名 | 重启 LLM 服务生效 |
| **本地 LLM → 角色词表补全** | 开 / 关（默认开，`llm.characterRepair`） | 关掉后不再把中文角色名补成 Danbooru tag，程序**完全不改**模型输出（应对常用词误命中） |
| **本地 LLM → 服务端口 / 上下文长度** | 1–65535 默认 8199 / 512–262144 默认 8192 | 重启 LLM 服务生效 |
| **本地 LLM → 单次回答上限** | 64–8192，默认 512 | 即时 |
| **本地 LLM → 系统提示词文件** | 只读展示 `assets/templates/anima-system-prompt.txt` | 改文件需重启后端 |
| **自检** | 「重新自检」列出全部问题，每条给出 `message → fix` | 即时 |

### 5.1 窗口行为两项已隐藏的说明（与启动器侧的托盘不是一回事）

设置页的「窗口」卡片**只有一段说明**，没有开关：

> 独立版是纯浏览器页面（后端进程 + 静态页面），**没有托盘进程**，因此「启动后自动最小化到托盘」与「关闭按钮行为」这两项不适用，故不提供。要退出请关闭启动器窗口或在窗口里按 `Ctrl+C`。

**为什么**：早期插件形态运行在宿主应用里，宿主有窗口与托盘可管；独立版是**「Node 后端进程 + 浏览器页面」**的组合，页面在系统浏览器里、进程在启动器窗口里，两者都不存在"托盘"这个东西，提供这两个开关只会是假开关。页面顶部另有"部分设置需重启面板后生效"的提示。

> **别混淆**：上面说的是**页面侧**（页面管不了窗口，所以不做假开关）；**启动器侧**（`scripts\start.ps1`）现在**确实**会起一个系统托盘图标并最小化控制台 —— 那是启动器行为，用 `-NoTray` 关闭，见 **F6**。

### 5.2 局域网开关与令牌

打开后：后端监听 `0.0.0.0`，并自动生成一个 **24 字符的十六进制令牌**（12 随机字节转 hex，显示在设置页"监听地址"卡片里）。局域网访问的两种带法：

```
http://<本机IP>:8788/?token=<令牌>          # 查询参数
X-DCP-Token: <令牌>                        # 请求头
```

启动日志的"局域网地址"行已自动拼好带令牌的完整 URL。

**边界/降级行为**：未开启局域网、或令牌为空时**不做校验**（只监听回环）；令牌不对时**非回环来源**的所有 `/app/*` 与 `/comfy-panel/*` 请求（含 WebSocket 握手）会收到 401，提示"局域网访问需要令牌：请在地址后加 `?token=<令牌>`（设置页可见）"。

> **v1.1.0 修复：回环来源不再要求令牌。** 令牌是给局域网内**其它设备**的（README §9.2 的口径），而旧实现对所有来源一律校验，于是"开一次局域网"会连锁触发两个真实故障：
> ① 设置页保存后**本机页面自己的**请求全部 401 → 外壳把整页换成「后端 API 不可用」，**令牌行反而永远看不到**（用户报的"开启局域网后看不到令牌"）；
> ② `scripts\start.ps1` 用 `http://127.0.0.1:<端口>/app/state` 探活、且不带令牌 → 一旦局域网开关被保存过，**下次双击 `start.cmd` 会等满 60 秒、报"后端在 60 秒内没有就绪"并把后端杀掉**。
>
> 现在：回环来源（`127.0.0.1` / `::1` / `::ffff:127.0.0.1`）一律放行，令牌只对真正的局域网设备生效；并且令牌会在**保存的那一刻**就显示在设置页（取自 `PUT /app/settings` 的响应，不需要刷新页面）。

---

## F6 控制台与托盘

> **这是启动器侧行为，与浏览器页面无关。** 页面本身仍然不提供"关闭按钮行为"之类的假开关（见 F5 §5.1）。

### 6.1 启动顺序

双击 `start.cmd` → `scripts\start.ps1`：

1. 从**脚本自身位置**推导项目根 → 找 Node → 创建 `logs\`、`data\`、`logs\jobs\`；
2. 后台起 `server\index.js`，最多等 **60 秒**就绪（按 `base..base+20` 探测 `/app/state`）；
3. **自动打开 Web UI**（`-NoBrowser` 可关）；
4. 拉起 `scripts\tray.ps1`（**独立进程**，`powershell -WindowStyle Hidden`）→ 常驻托盘图标；
5. **把控制台最小化到任务栏**：调 `user32!ShowWindow(hwnd, 6)`（`SW_MINIMIZE`）—— 是**最小化**不是隐藏，用户还能从任务栏找回控制台看输出；
6. 跟随后端进程，直到后端退出。

### 6.2 托盘菜单

| 菜单项 | 行为 |
|---|---|
| **打开 Web UI** | 用默认浏览器打开后端地址（**双击托盘图标同效**） |
| **复制访问地址** | 把 Web UI 地址写进剪贴板，并弹一个气泡提示 |
| **打开日志文件夹** | 用资源管理器打开 `<项目根>\logs` |
| **关闭控制台并停止后端** | 真正退出（**v1.2.2 改"先礼后兵"**）：先 `POST /app/quit` 请后端优雅退出（后端顺带停掉**本程序拉起的** ComfyUI 与本地 LLM、关 HTTP、落盘日志），最多等 8 秒；失败/超时才回退 `taskkill /T /F`（按 `BackendPid`，拿不到就按端口找监听进程）；**无论哪条路径**，最后都按 `data\run` 归属记录再清一次本程序拉起的 ComfyUI，然后销毁图标。托盘日志用 `graceful` / `forced` / `already-stopped` 互斥文案区分走了哪条路 |

**边界/降级行为**：

- 图标**取系统图标**（`SystemIcons.Application`），**不落任何资源文件、不写注册表、不引入第三方依赖**（只用系统自带 `System.Windows.Forms` / `System.Drawing`）；
- **后端退出后图标自动消失**：托盘进程每 3 秒看护一次后端（按 pid，退化到按端口监听），后端没了就自己退出，**不留幽灵图标**；
- 托盘自己的日志写在 `<项目根>\logs\tray.log`（记录"托盘已就绪"、退出原因、WinForms 加载失败原因等）；
- 拿不到 WinForms 时托盘进程直接退出（`exit 2`）并记日志，启动器**保持控制台可见**；
- 启动器最小化控制台失败时只打一条**警告**，不阻断运行。

### 6.3 `-NoTray` 开关

| 命令 | 托盘 | 控制台 |
|---|---|---|
| `pwsh -File .\scripts\start.ps1` | ✅ 有 | **最小化到任务栏** |
| `pwsh -File .\scripts\start.ps1 -NoTray` | ❌ 无 | 保持可见 |
| `pwsh -File .\scripts\start.ps1 -Foreground` | ❌ 无 | 后端日志直接打在当前控制台 |

`-NoTray` 同时关掉"装托盘"和"最小化控制台"两件事（需要一直盯着控制台输出时用）。

### 6.4 本轮修掉的一个真实缺陷：启动器路径不加引号

**现象**：项目路径含空格时（例如 `<盘符>:\...\COMFY UI\...`），启动器直接失败，node 报

> `Cannot find module '<盘符>:\...\<项目根前半截>'`（即被空格截断后的那半截路径）

**根因**：`Start-Process -ArgumentList` 是**按空格拼命令行**的，没给含空格的项目路径加引号时会被拆成两个参数，node 只拿到前半截。

**修法**：**所有**路径参数都加引号（后端脚本路径、托盘脚本路径、`-Url`、`-Root` 等）。

**边界/降级行为**：这是纯启动器侧修复，与浏览器页面无关；也正因为脚本只依赖相对路径，**换机器 / 换盘符后托盘与控制台最小化同样有效**。

---

## 与早期插件形态的差异对照

| 维度 | 早期插件形态 | 独立版（本项目） |
|---|---|---|
| **宿主依赖** | 必须装在宿主应用里，依赖宿主的插件系统与 `shell.overlay` 槽位 | **不依赖任何宿主**：自带 Node 后端 + 浏览器页面，双击 `start.cmd` 即可 |
| **前端生效方式** | 前端代码打进插件包，宿主侧有 `?rev=` 快照机制，改前端要重新加载快照/重启宿主 | **静态托管、无 `?rev=` 快照机制**：改 `web\` 下任何前端文件，**刷新页面即生效**，不需要重启任何宿主（响应头带 `no-store`） |
| **收藏持久化位置** | 浏览器 `localStorage`（key `dcp-artist-favs`） | **服务端 JSON**：`data\artists.json`（随项目文件夹迁移）；`localStorage` 只作为**一次性导入来源** |
| **画师自定义** | 无（只能从清单/收藏里选） | **可自由输入自定义画师**：自动规范化为 `@名字`，不做清单校验 |
| **黑名单** | 无 | **有**：图上「🚫 拉黑」+ 画师页黑名单管理；在全部随机档剔除，指定模式仍可搜到并标注；与收藏互斥 |
| **LLM 提示词** | 无 | **有**：内置 llama.cpp 运行时 + 预置 GGUF + 推荐模型目录，`/app/llm/*` 全套接口；「提示词工具」一键填入正/负向（真 React setState）+ 一键复制 |
| **推理来源：本地 or 外接 API** | 无（LLM 功能本身不存在） | **二选一**：「推理来源」= 本地模型（llama.cpp）或**外接 API（OpenAI 兼容）**；外接模式只需 baseUrl / Key / 模型名，只做纯聊天内核（见 F2 §2.9） |
| **角色识别** | 无 | **有**：Danbooru 角色词表（40,931 条角色 tag）+ 563 条内置中文别名 + 用户别名文件，模型漏角色时**补进正向提示词并显式告知**（见 F2 §2.10） |
| **下载镜像策略** | 由插件版安装器承担（PowerShell 侧） | 由 Node 后端统一承担（`server/download.js`）：超时 / 慢速 / **停滞**三档判定 + **hf-mirror / ModelScope / GitHub 代理 / pip** 四类镜像 + 候选来源列表入日志 + 明确提示 + 续传重试 + 逐源失败原因 |
| **托盘 / 进程管理** | 宿主提供窗口与托盘；ComfyUI 进程由面板 `POST /comfy-panel/launch` 拉起 | **启动器侧有托盘**（`scripts\tray.ps1` 独立进程：打开 UI / 复制地址 / 打开日志 / **先礼后兵地退出**），控制台最小化到任务栏；**页面侧仍不提供窗口开关**（F5 §5.1）；ComfyUI 子进程由 `server/comfy.js` 拉起并**按归属记录停止**（`stopOwned` / `killOwnedSync` / `cleanupOrphans`，只清本程序拉起的），可在页面看 `logs\comfyui.log` |
| **数据位置** | 分散在宿主插件数据目录 + 浏览器 `localStorage` | **全部在项目文件夹内**：`data\`（设置/收藏/LLM/**角色词表与别名**）、`logs\`、`models\`、`runtime\` —— **拷贝整个文件夹即迁移** |
| **面板复用** | 面板是插件的一部分 | 面板 `web\panel.js` 是插件版面板的**适配副本**，靠 `web\panel-host.js` 这个最小宿主层运行；HTTP 反代与 WebSocket 中继逻辑原样继承，`/comfy-panel/*` 兼容前缀**完全保留** |
| **界面语言** | 依宿主 | **中英双语**，设置页即时切换并持久化 |
| **版本自证** | 宿主插件版本号 | 侧边栏品牌区显示 `🎨 超低门槛 ComfyUI 工作流集成应用`，紧邻的副标题显示构建标记（当前 `v1.3.0`）与当前模式（内嵌/外接）；此外 `/comfy-panel/health` 与 `/app/state` 都返回 `buildTag`，`/app/state` 还返回**本程序实际监听端口**（顶层 `port`） |

---


### 2.11 外接 API：从"接不通"到实测可用（本轮修复）

| 现象 | 真实根因 | 修法 |
|---|---|---|
| 配好 baseUrl/Key/模型名后点「测试连接」像是不通、聊天框空白 | **思考型模型**（`deepseek-flash`）把整份 `max_tokens` 花在 `reasoning_content` 上，`content` 为空；同时"自检"只看 HTTP 状态、不看有没有正文 | 默认下发 **关思考**（`thinking:{type:'disabled'}` + `chat_template_kwargs.enable_thinking:false` 两种写法）；外接 `max_tokens` 默认 **4096**；自检在"只有思考没有正文"时**明确报错并给出下一步**，不再假通过 |
| 长回答中途断流 / 一直转圈 | 总超时（120 s）会把还在输出的长回答掐掉 | 改成**空闲看门狗**（默认 90 s 无数据才判卡死），总超时放到 300 s |
| 偶发 429 / 5xx 直接失败 | 无重试 | **退避重试**（默认 2 次，可在设置页调），重试过程在对话流里显示一条提示 |
| 模型名要手打、不知道服务端有什么 | —— | 新增 `POST /app/llm/api/models` + 设置页「获取模型列表」，下拉直接选；模型名不在服务端列表里会**明确警告** |
| baseUrl 写法五花八门（带不带 `/v1`、直接粘 `/chat/completions`） | —— | `normalizeApiBase()` 统一归一化，三种写法都接受 |

实测（DeepSeek `deepseek-flash`）：`/app/llm/api/test` 约 0.8 s 返回 `可以`；
`/app/llm/chat` 约 **1.2 s** 返回规范围栏 + 正/负向提示词，且把"初音未来"识别成 `hatsune_miku`；
接口级回归 `round3-api.cjs` **12/12 通过**（含"错 Key 要有可读提示且**不得泄露 Key**"这条）。

### 2.12 工作台三栏 1:1:2 与画师页独立

- 面板主体拆成三栏：**⚙ 生图设置（1）｜✎ 提示词 / 参考图（1）｜🖼 图片（2）**；
  列宽由 `grid-template-columns: 1fr 1fr 2fr` 决定，视觉顺序用 `order`（DOM 仍是原顺序）。
- **本地 LLM 提示词生成**用 `ReactDOM.createPortal` 挂进提示词栏里的 `.dcp-slot` 插槽：
  面板挂载后把插槽节点通过 `window.__DCP_PANEL_SLOTS__` + `dcp-panel-slots` 事件交给外壳。
- **画师 UI 独立成页**（左侧导航「画师」）：收藏 / 黑名单 / 按名字检索 / 从浏览器导入；
  面板里保留**画师模式**（关闭 / 大随机 / 小随机 / 随机收藏 / 指定）、**本次指定画师**、
  **收藏与拉黑计数**，以及「→ 去「画师」页管理」按钮（走 `dcp-go-tab` 事件切页）。
- 布局切换：**三栏 / 两栏 / 仅图片**（存 `localStorage: dcp-workbench-layout`，旧值 `gen` 自动归一化）；
  窄窗口纯 CSS 降级：≤1560px 变两栏且图片整行，≤1100px 单栏。
- 实测：`round3-ui.cjs` **16/16**（含"图片栏 ≈ 2×设置栏"、"视觉顺序 = 设置→提示词→图片"、
  "点入口切到画师页"、"仅图片模式下设置与提示词都收起"、"窄屏图片整行"、"默认模型 = anima-turbo-v1.1"）。


### 2.13 提示词生成：回车发送 / 四挡推理 / 上下文只留本地 / 逐条复制

| 项 | 行为 |
|---|---|
| **回车发送** | 输入框 Enter 直接发送；Shift+Enter 换行；输入法组合（`isComposing`）期间不发送 |
| **推理挡位** | off / low / high / max 四挡，设置页与对话页都能切；实测数据见 §2.11 与附录 K |
| **上下文策略** | 默认 `sendContext=false`：界面保留历史（本地默认 40 条，v1.2.1 起可调到 2000），发给模型的只有当前这一句；打开后按「上下文条数」发送最近 N 条 |
| **复制** | 每条消息右上角「复制本条」；对话底部「复制整段对话」（拼成"【用户】/【助手】"整段文本）；**v1.2.1 起只要有一条回答，「复制正+负」「复制整段回复」就可用** —— 旧版要求解析成功，模型没按围栏格式回答时两个按钮全是灰的（用户报的"生成内容无法一键复制"） |
| **思考过程可见** | v1.2.1：外接 API 的 `reasoning_content` 逐帧以独立字段下发，渲染成正文之前的「🧠 思考过程」块（等宽灰底、可折叠、可单独复制），与正文**完全分离**；后端只在"正文为空"时才把思考当回答返回，且不再重复发一遍 |
| **上下文不外发的可视化** | 对话标题行显示"上下文不外发"，底部一行写明当前策略，帧里带 `contextSent` |

### 2.15 切页面不丢状态（v1.2.1）

| 项 | 行为 |
|---|---|
| **工作台常驻** | 导航 = 四个常驻标签页（`.tab-panes > .tab-pane`）；切到设置/画师/向导时工作台**只隐藏、不卸载**，切回来零重载：正/负向提示词、参数、已出图、正在生成的进度、整段 LLM 对话都还在 |
| **为什么必须这样** | 旧版切页 = 卸载工作台 → 面板与 LLM 的 `useState` 全部重置；这是"从控制台切到设置就失去提示词和 LLM 历史"的根因 |
| **页面 key 必须稳定** | `renderPage` 的 key 只用 tab id（不能掺 `renderKey`）—— 按需加载完成时 `renderKey` 自增，掺进去会把刚挂载的页面整体重建，等于白常驻 |
| **轮询让路** | 标签页在后台时跳过 5 秒状态轮询（常驻工作台已经持有 ComfyUI 实时通道，再叠加轮询只会在切页前后堆半开请求） |
| **瞬时网络失败** | `fetchRetry`：幂等 GET 在**网络层 reject** 时最多重试 3 次；对话 POST 只在"发请求"这一步重试一次（流开始后绝不重试，避免界面上出现两份半截回答） |

### 2.14 画师页：本机作品

- **数据来源**：本机 ComfyUI `output` 目录（含 `<模型>\` 子目录），最多扫 4000 张、按时间倒序，15 秒缓存。
- **画师 tag 口径与面板一致**：文件名 `<画师>_<序号>_<批次>_.png` → `@画师名`；`noartist_...` 视为无画师。
- **展示与操作**：缩略图网格（点击开原图）、画师 chip 列表（点 chip 查该画师作品、点星星收藏）、
  每个画师与每张图上都能收藏；「打开图片文件夹」直接跳资源管理器。
- **没作品时**：明确显示「**本地没有产品**」，不留空白。
- **接口**：`GET /app/artists/works?artist=&q=&limit=`、`GET /app/output/file?name=&sub=`（直读磁盘，ComfyUI 离线也能看）、
  `POST /app/open-folder {which:'output'|'logs'|'models'}`。


### 6.5 第五轮修掉的真实缺陷：`start.cmd` 在中文 Windows 下双击必失败

| 项 | 内容 |
|---|---|
| 现象 | 双击 `start.cmd` 窗口一闪而过，服务没起；命令行里能看到 `'xxx' 不是内部或外部命令`，`EXIT=9009` |
| 根因 | 旧 `start.cmd` = **LF 换行 + UTF-8 中文注释**（47 个汉字/168 个非 ASCII 字节）。cmd.exe 按**OEM 代码页**（本机 **936/GBK**）读文件，UTF-8 汉字被解成乱码字节，其中部分字节序列**吃掉了行尾**，于是注释与下一行被拼成一条命令 → 解析失败 |
| 为什么以前没暴露 | 所有自动化验证都直接调 `scripts\start.ps1`（UTF-8 with BOM + CRLF，正确），**唯一没被自动化覆盖的就是"双击 `start.cmd`"这条入口** —— 恰好是用户最常用的入口 |
| 修复 | ① `start.cmd` 改写为**纯 ASCII + CRLF**（1190 字节 / 34 行 CRLF / 0 非 ASCII），中文提示交给 `start.ps1`；② `scripts/check.js` 新增 **[4b] 批处理编码红线**：`.cmd/.bat` 必须 CRLF + 纯 ASCII（无 BOM），否则自检失败；③ `.gitattributes` 已有 `*.cmd text eol=crlf`（但 git 不会改写已存在的工作区文件，所以**文件内容本身**必须写对） |
| 验证 | 用 `Start-Process <包>\start.cmd`（等价双击）实测：后端就绪、`GET /` = 200、托盘就绪日志写入、`logs\server.log` 有启动记录；自检从 16 项增至 **17 项全过** |

### 6.6 第七轮：下载体系的两个真实缺陷与镜像体系重做

| 项 | 内容 |
|---|---|
| 缺陷①：切页面就看不到安装进度 | 任务是服务端任务，但**向导页只把 job id 记在组件内存里**，离开页面即丢，回来只能看到空白。修复：新增 `GET /app/jobs`（列表 + `running`），向导挂载时按 `kind=setup` 重挂正在跑的任务；顶栏再放一个**全局**「⏳ 后台任务」chip（每 2 s 轮询，点开就是完整日志）——切到画师页/设置页也能看到进度与速度 |
| 缺陷②：**无论真实速度多少，面板都显示 0** | 根因不是前端：下载引擎里 `let got = startAt` 之后**再也没有任何地方给它加过字节数**（缺一个计数环节）。于是进度恒 0%、速度恒 0、ETA 恒空；更严重的是"连上后 N 秒一个字节都没动就判死换源"的看门狗读到 `got <= startAt` **永远成立**，会把正常下载在 20–60 s 时掐掉。修复：计数挂到 `pipeline` 的 `Transform` 上（不能用 `'data'` 监听——那会绕过背压）。这个 bug 藏了很久的原因是**结果是对的**（文件确实下下来了），只有过程指标全错 |
| 为什么现在才发现 | 本轮新增的"**10 秒内没有进展就换源**"规则要求"进展"可判定，一跑就把每个源都判成"只收到 0 B"——规则本身成了缺陷的检测器 |
| 镜像体系重做 | 镜像不再写死在代码里，而是 **`settings.download` 里的模板数组**：`hfMirrors` / `nodeMirrors` / `jsdelivrMirrors` / `githubProxies` / `extraMirrors`，占位符 `{repo} {owner} {name} {rev} {path} {file} {ver} {host} {url}`。换镜像 = 改设置，不用改代码 |
| 换源规则（用户要求） | ① **10 秒内没有正常开始下载**（已建连但收到的数据 < 64 KiB）→ 换下一个候选；② **连续 15 秒没有收到任何新字节** → 换下一个候选；③ 保留"整个窗口均速低于阈值"的竞争规则（最后一候选只按停滞判，避免所有源都慢时把能下完的文件掐掉）。三条阈值全部可在设置页调 |
| 镜像测速（新增） | 设置页 →「下载源 → 镜像测速」：粘贴任意直链，对**每个候选来源各真下 100 MiB**（文件不足则读全），表格列出首字节/已收到/速度/判定。走的是产品下载路径、不落盘、同一时刻只跑一个（`GET /app/download/speedtest`） |
| 角色 tag 规范化 | 外接模型常把 `rem_(re:zero)` 写成 `rem (re:zero)` 甚至 Markdown 转义的 `rem \(re:zero\)`。现在就地改成规范写法并经 `charactersFixed` 帧如实报告（`已把角色 tag 改成规范写法：rem (re:zero) → rem_(re:zero)`）；已是规范写法时一个字都不动 |
| 派生值不落盘 | `save()` 现在**不把派生出来的镜像梯队写进 `settings.json`**（除非用户显式设置过），并且**等于默认值的 `githubProxies` 也不落盘**。否则任何一个 `PUT /app/settings`（例如"测试连接"）都会把当时的默认梯队/代理列表**固化**进设置文件，之后升级默认镜像对这台机器彻底无效——实测踩过 |
| ComfyUI 便携包的完整性 | 便携包字节数与 sha256 由 `api.github.com` 的 `digest` 字段下发（本环境该 API 可直连），**经第三方代理下载也会校验**；官方渠道全不通时，才回落到"哈希校验通过的官方旧版镜像"（v0.3.59），再不行才是明确标注的第三方构建 |

## 文档版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| **v1.3.0**（第十二轮） | 本轮修复+新增 | **修的是用户报的安装阶段恶性 bug**：①**可续装** —— 逐组件/逐模型的安装状态落盘 `data/install/state.json`，且判定一律以**磁盘真值**为准（`server/install-state.js`：`verifyComponent` 检查 `main.py` + `python_embeded/python.exe` 是否真的都在）。修掉"每次点「开始安装」都重新安装 ComfyUI"（旧实现无条件 `fs.rmSync(runtime/comfyui)` + 全量解压 1.79 GB）；更严重的是那个目录**就是内嵌模式的权重目录**，重装会把用户已下好的模型一起删掉 —— 现在重装前先把 `ComfyUI\models` 临时挪走、解压成功后再原样合并回来，失败也照样放回（实测真跑过这条路径）。②**不再卡死** —— 任务日志实测"官方源超时→镜像，最后在 50–60 KB/s 上磨了 5 分钟、ETA 两万五千秒仍无中止"：`server/download.js` 新增**单来源墙钟上限**（模型 3 h / 组件 6 h，`download.maxTaskHoursModel/Component` 可调）与"无 content-length 时 30 s 无字节即换源"，并且**某组件/模型全部来源都无效时自动跳过、继续后面**（向导日志里明确列出跳过了谁、为什么）。另修两处真凶：`7z-extra.7z` 从 `down.npee.cn` 只下到 1.4 MB（真实 1.79 MB）却被判"下载完成"→ 7zr 报 `Is not archive` → 降级到 **7zr 精简版**去解大包（慢且难查），现在下载后先做**归档签名校验**、坏缓存直接删掉重下，7zr.exe 则改用**可执行文件头校验**（否则会把好好的 7zr 当坏包删掉，自检时真踩到）。③**组件与模型分开** —— 两条独立流水线：`runSetup` 分两段，模型段逐个 try/catch；安装中心另有 `component` / `model` 两类队列任务，互不触发。④**持久化下载队列**（新增 `server/install-queue.js` + `data/install/tasks.json`）—— 每个任务都能**暂停/继续/取消（取消会删本地文件与断点）/自动换源（清掉最快源记忆 + 跳过已失败主机 + 重新测速，保留断点续传）**，进度**关窗口甚至重启后端都还在**（重启后 running → paused，点「继续」续传）；默认串行，可上下移调序。⑤**逐个安装 + 进度** —— 新增**「📦 安装中心」页**（`web/pages/install.js`：前置组件 / 下载队列 / 模型库 / 自定义模型四块），模型每行一个安装按钮与进度条；向导页同样逐行给按钮与进度，并显示组件 ✅ 状态。⑥**前置自动入队** —— `installer/models.json` 每条新增显式 `requires`（可审计，不靠文件名猜），缺前置时界面提示「缺前置」并把前置一起排队，回报去重后的 `addedPrereqs`。⑦**自定义模型** —— 浏览器直传（原始字节直通，非 multipart）/ 从本机路径导入（**硬链接优先**）/ 只登记直链三种来源；可自定义命名、目标类型、配套管线与**文本编码器 / VAE**（保存时就校验家族一致，避免拖到生成时报维度/通道数错）；**与自带模型同名同文件也算独立的自定义模型**；生图面板新增 `customPairing` 纯函数，命中自定义模型时按用户声明的配对走（编码器/VAE/管线），并把它排到主模型下拉最前。⑧**硬编码的旧缺陷**：`fetchText` 收紧为单源 6 s / 最多 3 源；逐源测速 1 MiB→512 KiB、6 s→4 s 且**最多 3 路并发**（此前串行最坏要等一分钟）。⑨版本号 → **1.3.0**（`server/config.js` / `package.json` / 面板 `BUILD_TAG` 三处）。**验收**：`node scripts/check.js` **pass=19 fail=0**；沙箱端到端 harness **31/31** + 无头 Edge 界面验收 **20/20**（含"再次安装跳过本体""强制重装后占位权重仍在""取消删除本地文件""重启后队列恢复""前置自动入队去重"）；面板冒烟 `panel-test.cjs` **32/32**。 **本轮追加修正**：①**组件清单合并为两组** —— 向导与安装中心对用户只显示「前置组件」与「ComfyUI 本体」，组内具体装了什么（解压工具 / 画师清单 / 许可声明…）不再逐个暴露，每组各自一个「安装」按钮，进度按组汇总（`/app/models/library` 的 `groups`、`/app/install/queue` 的 `groups`；`/app/components/enqueue` 新增 `groups:[...]` 入参，`ids` 保留给排障）。②**Anima 3.8B 并入通用 Anima 管线** —— 面板不再有「Anima 3.8B」这个独立选项（`PIPELINE_RULES.anima` / `ROUTE_SHAPE.anima` 移除，`routeFor()` 只剩 Anima 通用与 Qwen-Image 2.1），`comfyui-anima-3-8B` 自定义节点因此**不再是前置**（`installer/models.json` 的 `requires` 去掉 3 处 `node:` 引用），该节点即使装不上也只记 warn、不会拖垮整组前置组件。 **第四批修正**：①**组件彻底合并成两条任务** —— 向导与安装中心都只显示「前置组件」与「ComfyUI 本体」，界面上**完全不再出现** 7-Zip / 画师清单 / 许可与第三方声明 / 自定义节点这些名字（`install-state.js` 的 `GROUPS` 把 5 个组件归成两组，`/app/models/library` 的 `groups` 与 `/app/install/queue` 的 `groups` 都只给组级信息；后端仍逐组件判定与安装，只是不再暴露）；②**双通道并行下载**（需求 2）—— 队列分成 `prereq`（前置组件 + ComfyUI）与 `model`（模型，默认 3 路并发）两条互不阻塞的通道：点一次「一键下载组件 + 模型」后，ComfyUI 便携包与模型**同时在下**，界面上的总速度是两者之和；ComfyUI 走"下载 → 解压"两阶段（`downloadPortableArchive` / `installFromArchive`），下载阶段一结束就把通道让给解压并继续下模型；**模型先下完会停在 99% 并显示「等待 ComfyUI」**，解压完成自动转成已安装；③**速度可见**（需求 3）—— `/app/install/queue` 新增 `totalSpeedKBs`（所有在跑任务速度之和）与 `runningSpeeds[]`，队列标题栏显示总速度徽标，每个任务行与每个模型行都显示自己的实时速度；④**向导页移除并首次启动自动跳转**（需求 4）—— 删除 `web/pages/wizard.js` 与 47 个 `wizard.*` 词典键，侧栏只剩「工作台 / 画师 / 安装中心 / 设置」；后端 `setup` 未完成且本机从未跳转过时（localStorage `dcp-first-run-install`）自动切到「安装中心」，该页顶部的引导横幅一键把「前置组件 + ComfyUI + 最小档模型」入队，并说明"两部分同时下载、模型先完会等 ComfyUI"。 **第五批**：①首次启动的按钮改为「**一键下载组件 + 默认模型**」，并且**只装默认模型**——`anima-turbo-v1.1.safetensors` + 它 `requires` 里声明的配套件（`qwen_3_06b_base.safetensors` 编码器、`qwen_image_vae.safetensors` VAE，合计约 5.24 GB，正好是面板默认管线开箱能出图的最小集）。模型集不再是"随便挑几个"，而是由 `models-catalog.js` 的 `DEFAULT_MODEL_ID` 与 `defaultModelIds` 统一定义并随 `/app/models/library` 下发（改这里就等于改"一键下载的模型集"）；其余 9 个权重留给用户在「模型库」按需点。②**「本地 LLM」统一改名为「LLM」**：侧栏、设置页栏目标题、就绪度提示与安装中心 FAQ 里的写法全部统一（词典里已无"本地 LLM"字样）。 **第六批（修用户报的两个 bug）**：①**「清空已结束任务」无效** —— 根因是**路由顺序**：`/app/install/queue/clear` 被前面那条 `/app/install/queue/<id>/<action>` 前缀分支抢先匹配，落进 `switch` 的 `default`，回报 **404「未知的队列操作：clear」**（实测复现：HTTP 404 且列表纹丝不动）。现已把 `clear` 判断提到前缀分支**之前**（`server/index.js`），并加了回归断言（接口 200 + `removed` + 列表按差量缩短 + **点界面按钮**也会真的清空）。②**ComfyUI 下载被挤占** —— 根因是**通道划分**：`prereq`（前置组件）与 `comfyui` 当时共用同一条 lane 且上限为 1，于是前置组件一开跑，ComfyUI 只能在队列里干等（实测：`prereq[running]` 时 `comfyui[queued]`，即使模型通道空着）。现已拆成**三条通道**：`comfyui`（独立、调度优先级最高）、`prereq`（默认 2 路）、`model`（默认 2 路），三通道并行 —— 实测同屏出现 `prereq(prereq) + comfyui(comfyui) + 3 个模型(model)` 同时 running。另补防御：取消会清掉 `archivePath` 并把 `phase` 退回 `download`（避免拿一个已被删掉的归档去解压）、点「继续」会清空上一轮累积的「跳过这些来源」。 **第七批（修用户报的 bug）**：**「ComfyUI 本体」那一行没有下载速度**。根因是 `downloadPortableArchive()`（第六批把 ComfyUI 拆成"下载 / 解压"两阶段时新写的那条下载路径）**漏传了 `onProgress`** —— 其余下载路径（模型、7-Zip、节点）都传了，唯独这条 1.79 GB 的主下载没传，于是队列拿不到 `downloaded/total/speedKBs/etaSec`，界面只能显示"下载中 0%"。已补上，并**修正百分比公式**：不再照抄下载引擎按"目录里写死的期望字节数"算出的百分比（实测 1.79 GB 的包下到 457 MB 时它仍报 0%），改为队列自己按 `downloaded/total` 计算、下载阶段折算到 0–90%（最后 10% 留给解压），新增 `downloadPercent` 字段；界面对大包显示一位小数百分比 + 「已下 / 总量」+ 实时速度。新增回归 `.scratch\_v213-b7.cjs`（本地起一个 1.79 GB / 25 MB/s 的假镜像，经队列下载并断言速度、百分比、candidate、总速度都在），**9/9**。 |
| **v1.2.2**（第十一轮） | 本轮修复 | ①**退出时连带停掉本程序拉起的 ComfyUI**：新增 `POST /app/quit`（停 ComfyUI → 停 LLM → 关 HTTP → 落盘 → 退出；重复/并发只收尾一次），`SIGINT/SIGTERM/SIGBREAK` 走同一套，`process.on('exit')` 加**纯同步**兜底 `killOwnedSync`；托盘「关闭控制台并停止后端」改为**先礼后兵**（先请求优雅退出，6 s 请求超时 + 最多等 8 s，失败才回退 `taskkill /T /F`，两条路径最后都按归属记录再清一次）。*（Windows 上 `taskkill /F` = `TerminateProcess`，实测不给 node 任何执行机会 —— "先礼"是必需项。）* ②**只清自己的**：新增进程归属记录 `data\run\comfy-owner-<pid>.json`（`launch()` 成功即写、子进程退出即删）+ **五条谓词**（记录可解析 → 进程存活 → 命令行含 `main.py` → 命令行反推入口目录 == 记录 `codeDir`（或含记录 `mainPy`）→ pid ≠ 自身），任一条不满足**只记 WARN、绝不动手**；启动时 `cleanupOrphans()` 只清自己写过的记录；**删除**旧 `stop()` 里"按 `comfy.port` 找监听进程再杀"的兜底（那会误杀用户自己启动的实例），非本程序拉起的实例改为回报 `owner=foreign` 并跳过；判据从"端口是否在线"改成"**子进程是否还在**"（自己拉起的实例可能已离线但进程仍在，退出时照样要停）。③**孤儿真因（实测纠正）**：不是 `detached: true` 让 `/T` 抓不到（父进程还活着时 `/T /F` 能连 detached 子进程一起收），而是**父链断裂** —— `scripts\start.ps1:141/206` 的 `$proc.Kill()` 是单进程杀，控制台被强杀时中间那层 node 先死、ComfyUI 以 detached 子进程存活，此后 `/T` 再也够不着，下次启动又被 `launch()` 的"端口有回应就返回 `online:true`"认领；放大器是旧 `listen()` 在 EADDRINUSE 自增时**重复注册就绪回调**（`autoStart=true` 会一次拉起 3 个 ComfyUI，内存只记住最后一个 pid）—— 一并修掉。④**端口不再漂移**：`MAX_PORT_BUMP=2`、每次自增 WARN、超限 `log.error` + `DCP_PORT` 建议 + `exit 1`；**取消** `save({listen:{port:actual}})` 回写（`listen.port` 永远是配置值），实际端口由 `/app/state` **顶层 `port`** 自证（与「打开：」日志、`DCP_READY` 一致）。⑤**前端不刷屏**：顶栏「服务地址」徽标（`data-dcp-service-port`；取值链 顶层 `port` → `listen.port` 兜底并换 tooltip → 不渲染；空串/NaN/非整数/<1/>65535 一律当缺失，绝不出现 `undefined/NaN`）；断连只在"连通→断连"跳变画**一条**可操作 banner（写明本页可能不是服务实际地址 + 托盘「打开 Web UI / 复制访问地址」/ 服务端「打开：」/ `logs\server.log` 三条途径 + 复制地址/立即重试），恢复即消失，首帧连不上改为 5 s 轻探自愈；同文案 error toast **30 s 节流**；`__DCP_SAVE_ARTISTS__` **单飞 + 按 key 合并、只在真变更时广播**（空闲自我回声约 166 次/秒 → 0~1 次/22 s）。⑥版本号 → `v1.2.2`（三处一致）。**独立复核**：`node scripts/check.js` **pass=19 fail=0**、后端 harness **105/105**、前端无头 Edge **45/45**（`docs/ROUND11-VERIFY.md`）；已知边界 A–E 见 `docs/HANDOVER.md` §13.2。 |
| **v1.2.1**（第十轮） | 本轮修复+新增 | ①**切页面不再丢状态**：导航改成四个**常驻标签页**（`.tab-panes > .tab-pane`），切到设置/画师/向导时工作台只隐藏、**不卸载** —— 修掉"从控制台切到设置就失去提示词与 LLM 历史"（根因是 `renderPage` 的 key 里掺了 `renderKey`，按需加载完成时把刚挂载的页面整体重建）；后台标签页跳过 5 秒轮询，幂等 GET 加瞬时失败重试。②**画师自定义分组补齐**：收藏/黑名单 chip 上加「＋分组」，分组卡片可展开成员、逐个「移出本组」、**重命名**；**生图面板内生图这一侧**新增 🗂 加组入口（指定画师下拉行 / 已选 chip / 分组随机块，不切页）；并修掉"面板挂载时把分组写回空快照"的回归（`persistArtists`/`__DCP_SAVE_ARTISTS__` 改为按 key 传、按 key 写，新增 `POST /app/artists/groups/replace`）。③**外接 API 的思考过程可见 + 复制可靠**：`reasoning_content` 逐帧以独立字段 `reasoning` 下发，前端渲染成正文之前的「🧠 思考过程」块（可折叠、可单独复制，与正文完全分离）；后端"只有思考没有正文"时不再重复发一遍文本；前端复制按钮不再依赖解析成功（只要有一条回答就能复制），并修掉 `setLast` 打在 system 说明帧上导致助手气泡恒为空、复制按钮全灰的真实缺陷。④**历史永久保留**：`新建会话` 不再 `dropSession`（新增 `openSession`），新增 `GET /app/llm/sessions`（只给摘要）、`DELETE /app/llm/session/:id`（唯一真删除入口）、`lastSessionId` 记录与「接回上一次对话」；侧栏新增「历史对话（永久保留）」卡片（点开载入 / 两步确认删除）；`keepMessages` 上限 200 → **2000**。⑤版本号 → `v1.2.1`。**实测**：用「日常使用版」的 anima-turbo-v1.1 + qwen_3_06b + qwen_image_vae 真出 1 张 1024×1024 PNG（10 步 / CFG 1 / er_sde+simple，28.3 s，`output/v121-accept/noartist_1_00001_.png`），测试后已停实例并删除测试输出；Edge CDP 浏览器验收脚本 16/18（两条失败是脚本自身的时序断言，已在 `docs/HANDOVER.md` 第 10 轮记录里说明）。 |
| v1.0.0 | 首次编写 | `FEATURES.md` 首次成文；按 F1–F5 逐条展开「做什么 → 怎么用 → 边界/降级行为」，并补齐与早期插件形态的差异对照。所有行为均对照 `server/`、`web/`、`installer/models.json` 实际实现撰写。 |
| v1.0.0（第二轮） | 本轮补充 | 按第二轮改动扩写：F1 增加「三栏工作台」；F2 扩成**本地模型（推荐目录）/ 外接 API / 角色词表 / 提示词工具（填入+复制）**四块；F3 补 ModelScope、直链规范化、窗口均速 + 停滞双阈值、UA/Range/看门狗；新增 **F6 控制台与托盘**（含 `-NoTray` 与启动器引号缺陷）；对照表补「推理来源：本地 or 外接 API」与「角色识别」两行，并改写下载与托盘两行。**如实标注**：9B 默认模型的下载与加载运行验证尚未完成。 |
| v1.0.0（第二轮补强） | 本轮追加 | 内置中文别名从 101 条扩到 **563 条**（覆盖 Re:Zero / FGO / 东方 / VOCALOID / 原神 / 星穹铁道 / 绝区零 / 鸣潮 / 明日方舟 / Blue Archive / 赛马娘 / hololive / nijisanji / 火影 / 海贼 / 龙珠 / 美少女战士 / 魔卡少女樱 / P5 / 赛博朋克 / 守望先锋 / LOL 等主流作品），**每一条都用 `danbooru.csv` 逐条核对**（583 条候选 → 删 20 条 → **563/563 精确命中**，运行时自检 `aliasMisses=0`）；刻意排除「时 / 天天 / 天使 / 真理 / 琴 / 小美 / 白露 / 悠悠 / 陈 / 玛丽 / 吉尔 / 忧」等常用中文词；新增设置项 `llm.characterRepair`（角色词表补全开关，默认开）。 |
| v1.0.0（第二轮终检） | 本轮追加 | 9B 默认模型的**下载 → sha256 校验 → 加载 → 推理**全链路实测（8 GB 显存、`-ngl 99`、12.3 s 就绪、角色问题答 `rem`）；据此把 `installer/llm-models.json` 的 7 条都补上 **sha256**，下载后自动流式校验、不一致删文件报错（`server/download.js` 的校验路径本轮首次真跑），新增 `POST /app/llm/models/verify` 与「校验完整性」按钮；记录"字节数相符但内容损坏"这一真实陷阱与 `modelscope:false` 条目的候选裁剪。 |
| v1.0.0（第三轮） | 本轮追加 | 外接 API 修复（思考型模型吃光 token 预算的根因、关思考、4096 上限、空闲看门狗、退避重试、模型列表与预设；DeepSeek `deepseek-flash` 实测 12/12）；工作台改版为 **生图设置 1 : 提示词 1 : 图片 2** 三栏、本地 LLM 用 portal 挂进提示词栏、**画师 UI 独立成页**（导航四项）、布局切换与窄屏降级（16/16）；默认生图模型改为 **anima-turbo-v1.1.safetensors**；提示词生成默认走**外接 API**；本地模型只留 **4B（已从原目录迁移进项目）**，9B/2B/DanTagGen 卸载；推荐模型改为**只给链接**（9B/4B/2B）。 |
| v1.0.0（第四轮） | 本轮追加 | 回车即发送；外接 API **四挡推理**（off/low/high/max，实测数据见附录 L）；**上下文只留本地、默认不外发**（历史可整段复制）；工作台重排（LLM 在最上、提示词工具第二、提示词/参考图进左栏、主图变小历史变大、跳转图片文件夹）；默认画师 **大随机**；**切模型不再清空提示词与画师设置**；画师页新增**本机作品**（缩略图 + 一键收藏 + 搜索 + 「本地没有产品」空态）。 |
| v1.0.0（第五轮） | 本轮修复 | 修复**双击 `start.cmd` 启动失败**的真实缺陷：`start.cmd` 由「LF + UTF-8 中文注释」改为**纯 ASCII + CRLF**（cmd.exe 按 OEM 代码页读批处理文件，中文注释会吃掉行尾 → exit 9009）；`scripts/check.js` 新增 **[4b] 批处理编码红线**（自检 16 → **17 项**）；验收补上「双击等价启动」（`Start-Process <包>\start.cmd`）；MIGRATION 新增 §13 给其他设备的两条硬约束。 |
| v1.0.0（第六轮） | 本轮调整 | 最小下载挡位的生图模型改为 **`anima-turbo-v1.1.safetensors`**：`installer/models.json` 里 turbo 由 standard 提到 **minimal**、`Anima-3.8B-v1.1` 与 `qwen35_4b` 降到 standard，最小挡位 **14.00 → 5.24 GB**（正好等于"面板默认配置开箱能出图"的最小集），standard / full 不变。 |
| **v1.2.0**（第九轮） | 本轮修复+新增 | ①**删除历史照片**：图片栏（缩略图 / 当前图）与画师页「本机作品」都能删；**两步确认且按钮写明文件名**；web UI 与磁盘一起删、被删空的模型子目录一并收掉（只允许删 output 目录内的图片）。②**画师分组（最多 50 组）**：画师页新增分组卡片（建组 / 看成员数 / 删组），画师行上「＋分组」点开列出已存在的组、选一个即加入（再点一次移出，也可就地新建并加入）；工作台新增 **「🎲 分组随机」+ 具体某个组的下拉**（显示组名与人数、本组可用 N/M、黑名单照旧跳过）。③**输出目录跟随实际在跑的 ComfyUI**：新增 `comfy.outputDir` 覆盖；留空时「本程序拉起的实例 → 监听端口进程的命令行反推 → 按模式推导」三级解析，设置页可「检测」看结果与图片数 —— 修掉"生成的照片根本不出现在图片文件夹里"（外部自建 ComfyUI 场景）。④**API Key 改成只写字段**：`/app/settings` 只回 `hasKey` 不回显、留空保存不清 Key、显式「清除 Key」按钮 —— 修掉"切一下推理挡位就得重新输入 API Key"。⑤**下载前逐源测速再固定用最快源**：每源探 1 MiB/最多 6 s，按实测速度排序；同"来源家族"10 分钟内沿用最快源（不再来回切换），中途停滞仍按原有换源规则退到下一个候选。⑥版本号升到 `v1.2.0`。 |
| **v1.1.0**（第八轮） | 本轮修复+新增 | 对照用户提交的 `BUGS-AND-FIXES.md` 逐条核对并修复（B5/B6/B7/B8/B9 五条均复现并已修）：B6 推理来源判定（对话能发出去、顶栏显示 `deepseek-flash`）；B7 局域网令牌可见 + **回环来源不再要求令牌**（否则开一次局域网就会把本机页面和 `start.cmd` 探活全挡死）；B8 `api()` 纯对象 body 自动序列化（10 处按钮恢复）；B9 自检按来源条件化；B5 向导 `setup.json` 记账补回 `skipped`。另**恢复面板「🔍 指定画师」的搜索与下拉**并新增**收藏画师搜索**（仅收藏范围点开即列全部收藏、输入过滤、点选即用）—— 详见 §2.9（就绪度按来源判定）、§4.4（面板内的指定画师搜索与收藏画师搜索）、§5.2（局域网令牌与回环放行）；另新增 **`AI-DECLARATION.md`**（AI 生成声明，随核心版交付）。 |
| v1.0.0（第七轮） | 本轮修复+新增 | 见 §6.6：向导进度重挂 + 顶栏全局任务条（缺陷①）；**下载字节计数从未生效**导致进度/速度恒 0 且看门狗误杀（缺陷②，根因级修复）；**截断下载防线**（改名之前校验字节数：ModelScope 实测会把 242 MB 下成 126/121/112/3 MB 而流"正常结束"）；镜像梯队全部改为设置项（模板占位符化）+ 三条换源规则；新增**镜像测速**（100 MiB × 每源）；角色 tag 规范化（含 Markdown 转义写法）；`save()` 不再把派生梯队落盘；新增 **`docs/HANDOVER.md` 项目交接文档**。**如实标注**：GitHub 系资源（ComfyUI 便携包 / llama.cpp）在该环境只有 `gh-proxy.com` 与 `down.npee.cn` 两个快源，4.2 GB 的 `anima-turbo-v1.1` 也只有 ModelScope 是快源。 |
